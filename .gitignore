bin/
obj/
.env
.env.prod
.pointer/*
!.pointer/credentials.env.example
# Anchored: un-anchored, this also hides e2e/dashboard/ and any other nested dashboard dir.
/dashboard/
*.user
.superpowers/
API/wwwroot/uploads/
.playwright-cli/
.playwright-mcp/
node_modules/
/clients/
openapi.json
.claude/worktrees/

# Delegation briefs. Written into a worktree when handing work to another agent; scratch by
# definition, and one was committed by an `add -A` before this line existed.
TASK.md

# landing redesign scratch (pipeline logs, staging dirs) and impeccable screenshot evidence
landing-redesign/**/*.log
landing-redesign/**/.tmp/
.impeccable/review/
