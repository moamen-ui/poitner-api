{
  "name": "pointer-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Pointer feedback — Chrome/Chromium browser extension (MV3)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "@types/chrome": "^0.0.287"
  },
  "allowScripts": {
    "esbuild@0.24.2": true
  }
}
