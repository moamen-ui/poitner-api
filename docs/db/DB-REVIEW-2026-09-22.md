# DB review — 2026-09-22

Reviewer: db-architect (read-only pass over `main` @ `03093ad`). Stage: **pre-launch** — one real
workspace in production, owner willing to reshape the schema now. Findings are ordered by **cost if
delayed**, not by how "broken" they are. Every finding cites the file:line or migration id it was
verified against today.

Companion files: [`DB-RULES.md`](DB-RULES.md) (standing rules), [`SCHEMA.md`](SCHEMA.md) (table map),
[`execution/`](execution/) (one doc per endorsed recommendation).

**Amended 2026-09-22 (evening)** after the owner's decisions (Q2 = abort, Q3 = own workspace
name, Q4 = SET NULL, Q8 = defaults + enabled, DB-07 approved, Q1 narrowed to four providers) and
after DB-05 deployed, DB-10 merged (CI green) and DB-09 merged. §7 (table, order, **batching
verdict**) and §8 are current as of `ff25a8d`; a new [DB-03b](execution/DB-03b-workspace-name-surface.md) exists.
**Amended 2026-09-22 (same day, later)** after two independent cross-reviews
([`reviews/REVIEW-AGY-2026-09-22.md`](reviews/REVIEW-AGY-2026-09-22.md),
[`reviews/REVIEW-GLM-2026-09-22.md`](reviews/REVIEW-GLM-2026-09-22.md)) and after DB-02 and DB-04
shipped. Amended cells are marked *(amended)*; the adjudication is §9. Line references in the
finding tables are still as of `03093ad` unless a cell says otherwise.

---

## 1. Snapshot

| Item | Value | Evidence |
|---|---|---|
| Engine | PostgreSQL **15** (official image), one container, one named volume `pgdata` | `docker-compose.prod.yml:6,12,69`; dev `docker-compose.yaml:3,8` (host port 5433) |
| ORM | EF Core 8 + `Npgsql.EntityFrameworkCore.PostgreSQL` **8.0.11**; connection retry 5×/30 s, pool 40 | `Infrastructure/Pointer.Infrastructure.csproj:14`, `Infrastructure/DependencyInjection.cs:27-30` |
| Tables | **25** (all `snake_case`, schema `public`) + `__EFMigrationsHistory` | `Infrastructure/Migrations/AppDbContextModelSnapshot.cs` (`ToTable` calls); the rebranding plan §11.1 still says 18 — stale |
| Migrations | **58** as of `03093ad` (7 in Jun, 12 Jul, 12 Aug, 27 Sep 2026); **60** as of `ff25a8d` (DB-04 `20260922080137_DropShadowProjectAppUrlProjectId1`, DB-05 `20260922082557_SoftDeleteAwareUniqueIndexes`, both applied in production and both carrying `[ContractMigration]`) *(amended)*; all migration ids frozen (`Tests/MigrationSafetyTests.cs:13-73`) | `Infrastructure/Migrations/`, `git log`; `REBRANDING-PLAN.md` §4.1 |
| PK / types | `int identity` PKs everywhere; `uuid` for user/tenant identity (109 cols); `timestamp with time zone` for every timestamp (97 cols); 7 explicit `jsonb` + 5 owned `ToJson` columns; no `timestamp without time zone`, no `text` PKs | snapshot `HasColumnType` census |
| How migrations reach prod | **Auto-applied on API boot** (`MigrateAsync` → `AdminSeeder` → `ApiKeyBackfill`) when `DBMigrationEnabled=true`, which prod sets | `API/Program.cs:165-174`, `docker-compose.prod.yml:26` |
| Deploy path | `scripts/deploy-api.sh` on the VM: `git pull` → **`backup-db.sh pre-deploy`** → `compose up -d --build api` → smoke | `scripts/deploy-api.sh:19-43` |
| Backups | `scripts/backup-db.sh`: `pg_dump -Fc -Z6` to `~/backups/pointer-<ts>[-label].dump` on the **same VM disk**; magic-byte check; 14-day retention, always keeps newest 3; nightly cron 03:00 UTC (installed 2026-09-22) | `scripts/backup-db.sh:24-45`, `DEPLOY.md:91-109` |
| Restore | Written (`DROP DATABASE` → `CREATE` → `pg_restore --no-owner --no-privileges`); **rehearsed locally 2026-09-22** (dump `pointer-20260922T071358Z-initial.dump` → scratch `pointer_rehearsal`, 26 tables, 58 history rows, row counts matched); **production restore still unexercised** *(amended)* | `DEPLOY.md` § Restore "Last rehearsed" |
| Off-box copy | **None** | `DEPLOY.md:107-109` says so |
| Uploads (screenshots) | Docker volume `uploads` → `wwwroot/uploads/<ownerId>/<projectKey>/…`, referenced from `comments.element.ScreenshotUrl`; **not in any backup** | `docker-compose.prod.yml:50,70`, `Infrastructure/Storage/LocalFileStorage.cs:14,26` |
| Row scale | Unknown. Last hard number: 104 comments on 2026-09-15 (`20260915163653_FixPayloadFlagsJsonDefault.cs:42`). One workspace. Treat everything as "small" — every table can be rewritten in a single transaction today. | — |
| CI vs. database | CI runs `dotnet test` only (InMemory + Sqlite providers). **No job applies the migrations to a PostgreSQL instance**; the e2e job starts only Verdaccio. First contact between a new migration and Postgres is production boot, unless the developer ran `just up` locally. *(amended: fixed by **DB-10** — `postgres:15` service job, apply-from-empty + drift check + Down/Up round-trip)* | `.github/workflows/e2e.yml:29`, `e2e/run-e2e.sh:208`, `Tests/Pointer.Tests.csproj:14-15` |
| Tenancy tests | 12 filter tests + 5 ownership + 3 super-admin-blocked | `Tests/TenantQueryFilterTests.cs`, `WorkspaceAdminOwnershipTests.cs`, `SuperAdminUsageBlockedTests.cs` |

---

## 2. P0 — data safety

| # | Sev | Finding | Evidence | Consequence | Fix |
|---|---|---|---|---|---|
| P0-1 | **P0 → P2** *(amended)* | Restore procedure **rehearsed locally 2026-09-22** (`DEPLOY.md` § Restore "Last rehearsed": the exact `pg_restore` line restored the initial prod dump into a scratch database; 26 tables, 58 history rows, counts matched). **Production restore still unexercised** — the VM-side sequence (`stop api` → `pre-restore` dump → `DROP/CREATE` → `pg_restore` → `up -d api`) has not been run against the real container. | `DEPLOY.md` § Restore | Residual risk is the environment delta (compose `exec -T` piping, volume permissions), not the `pg_restore` invocation. | Repeat the rehearsal whenever `backup-db.sh` or the restore steps change (R11); run the VM-side sequence once on a **scratch database name** on the prod container the next time the API is stopped for an R7 step. DB-01 is re-scoped to off-box copy + uploads + freshness gate. |
| P0-2 | **P0** | All dumps live on the VM's own disk. | `scripts/backup-db.sh:16` (`~/backups`), `DEPLOY.md:107-109` | Loss of the VM/volume = loss of every backup at once. | **DB-01**: nightly off-box copy (rclone → S3-compatible bucket). |
| P0-3 | **P0** | The `uploads` volume (comment screenshots) is not dumped, copied or listed in the restore procedure. | `docker-compose.prod.yml:50,70`; `backup-db.sh` dumps only Postgres | A restore brings back comments whose screenshots 404 forever; `comments.element.ScreenshotUrl` is history that cannot be regenerated. | **DB-01**: tar the volume next to each dump; include it in the off-box copy and the restore steps. |
| P0-4 | **P0** | Auto-migrate on boot has already shipped **destructive / data-moving `Up()` bodies** with no gate: two columns dropped without backfill (`20260701163243_MultiSelectPickedActions.cs:13-19`), expand-backfill-**contract in one migration** (`20260903191419_ProjectPerEnvironmentActivation.cs:37-42`), NOT NULL enforced with a `gen_random_uuid()` backfill that would have minted unreachable tenants for any straggler (`20260827130246_EnforceOwnerIdNotNull.cs:24-31`), data-only migrations with an **empty `Down()`** (`20260911170828_MigrateDefaultProjectAppUrlsToLocal.cs:48-51`). | migration ids as cited; `API/Program.cs:169` | A future migration of the same shape runs unattended on first request after `compose up`; the pre-deploy dump is the only undo, and rollback also requires checking out the previous commit (`DEPLOY.md:128-130`). | **DB-02** *(shipped 94ebf27)*: guard test that fails the build when a *new* migration's `Up()` contains `Drop*`/`Rename*`/narrowing `AlterColumn`/`.Sql(` without an explicit `// DB-RULES:` approval marker; **DB-RULES R7** restricts boot-time apply to additive migrations. *(amended)* The marker documents approval but does not stop `MigrateAsync` (GLM B2 / AGY 1.4): **DB-09** adds the mechanical gate — `[ContractMigration]` attribute next to the marker, `Program.cs` refuses to auto-apply a pending marked migration unless `DBApplyContractMigrations=true`, which only the explicit `POINTER_APPLY_CONTRACT=1` path of `deploy-api.sh` sets (API stopped, labelled dump). *(evening)* **DB-09 merged `ff25a8d`; prod deploy pending (ordinary run).** |
| P0-5 | **P0** | No environment other than production ever runs the migration chain against PostgreSQL with real data shapes. InMemory ignores `HasFilter`, value converters and FK behaviour (`Tests/CommentFieldsTests.cs:21-24` admits this); Sqlite (`Tests/UsageEventFirstCommentTests.cs:43-60`) uses `EnsureCreated`, not the migrations. | `.github/workflows/e2e.yml:29`, `Tests/Pointer.Tests.csproj:14-15` | Raw-SQL mistakes (a `jsonb` default of `'{}'` where `'[]'` was needed — `20260915163653` was the repair for exactly this, 104 rows broken in prod) are found by customers. | **DB-RULES §Local rehearsal**: every migration is applied to a restored copy of the latest prod dump before merge — exact command in the rules. *(amended)* Q7 answered yes → **DB-10**: a GitHub Actions job with a `postgres:15` service that applies every migration from empty, checks the snapshot has no pending model changes, round-trips the newest `Down()`/`Up()`, and runs `MigrationSafetyTests`. *(evening)* **Merged `827cfd8`, first run green (35706559115).** P0-5 is closed for the "never ran against Postgres" half; the "real data shapes" half remains the R11 rehearsal. |

---

## 3. Structural findings

| # | Sev | Finding | Evidence | Consequence | Fix |
|---|---|---|---|---|---|
| S-1 | **P1** | **There is no workspace/tenant table.** A workspace is a `uuid` (`owner_id`) that equals the founding admin's `users.public_id`; identity is recovered at runtime by matching `roles.name == "Workspace Admin"` (45 references to the role-name constants across `UserService.cs:19-20`, `TenantService.cs:19`, `InviteService.cs:24-25`). Name resolution is "the current admin's display name" (`AuthService.cs:157-170`). Four services mint a workspace by copying `publicId` into `OwnerId` (`AuthService.cs:407-418`, `TenantService.cs:171-182`, `InviteService.cs:602-613`, `DemoService.cs:89-100`). **No `owner_id` column anywhere has a foreign key.** | as cited; `TenantService.ListAsync` `TenantService.cs:36-45` | (a) an `owner_id` typo/bug creates an unreachable tenant silently — `20260827130246` relied on this being impossible by code review only; (b) workspace attributes (name, demo TTL, caps, `recipient_email`, planned §32 webhook URL, planned §19 billing owner) have nowhere to live except the admin **user** row (`User.cs:43-56`) or a one-row satellite table (`workspace_settings`, `WorkspaceSetting.cs:6-10` documents the workaround); (c) ownership transfer leaves `owner_id` pointing at a `public_id` that is no longer the admin (`TenantResponse.cs:8-11`), so the "owner is a user" reading is already false; (d) see S-2. | **DB-03**: add `workspaces (id uuid PK = today's owner_id values, name, timestamps, soft-delete)`, backfill from the Workspace Admin rows, add `owner_id → workspaces(id)` FKs to all 23 owner-carrying tables, mint the row at the four mint points, and make `HardDeleteAsync` complete. Keep the JWT `tenant` claim and every `OwnerId` property unchanged (zero DTO churn). Do it **now**, while one workspace exists. |
| S-2 | **P1** | Tenant hard-delete (`TenantService.HardDeleteAsync`, `TenantService.cs:356-441`) deletes **6** tables (replies, comments, projects, status_presentations, users, roles) out of **23** that carry `owner_id`. `DemoCleanupService` calls it hourly for expired demos (`API/Hosted/DemoCleanupService.cs:20-24,78`). Orphaned on every run: `predefined_actions`, `predefined_action_suggestions`, `invites`, `quick_access_links`, `subscriptions`, `extension_sites`, `ai_rules`, `workspace_settings`, `role_tenant_overrides`, tenant-owned `app_environments`, `device_logins`, `usage_events`. Worse: `notifications.project_id` is `ON DELETE RESTRICT` (`NotificationMapping.cs:34`) and suggestion notifications have `comment_id = NULL` (`Notification.cs:10,19`), so a workspace holding **any** suggestion notification cannot be deleted at all — the `RemoveRange(projects)` throws an FK violation and the sweep logs failure every hour. | as cited | Demo tenants that used suggestions are never cleaned up; deleted tenants leave subscription and settings rows behind; `subscriptions.owner_id` is unique, so a re-created tenant with the same uuid is impossible anyway, but the rows still count toward nothing and confuse operator queries. | **DB-03** (ordered delete list + reflection test that every `OwnerId`-carrying entity is in it) and **DB-06** (real FKs so project-scoped children cascade). |
| S-3 | **P1 — shipped** *(amended: DB-04 merged 7a015cb, deployed to production 2026-09-22 08:18 UTC via the R7 explicit step — api stopped, dump `pre-db04`, pre-check 0, 15 rows unchanged, smoke 200)* | **Shadow column `project_app_urls."ProjectId1"`** (PascalCase, nullable int, with its own index `IX_project_app_urls_ProjectId1` and FK `FK_project_app_urls_projects_ProjectId1`) exists in production. Cause: `ProjectAppUrlMapping.cs:22` says `.WithMany()` while `Project.cs:61` declares an inverse collection `ProjectAppUrls`, so EF built a second relationship. Shipped in `20260911223426_AddTenantInviteFields.cs:14,33-40` (unrelated to that migration's name — nobody read the generated file). | snapshot `AppDbContextModelSnapshot.cs:1268,1291,2400-2402` | `Project.ProjectAppUrls` is always empty when loaded (it follows the NULL shadow FK). Nothing in `Application/` uses that navigation today (only `AppEnvironment.ProjectAppUrls` is used, `AppEnvironmentService.cs:39,162`), so it is latent — until someone `Include`s it. Also a textbook case for the "read the generated migration" rule. | **DB-04**: fix the mapping to `.WithMany(p => p.ProjectAppUrls)`, drop the column/index/FK after verifying `COUNT(*) WHERE "ProjectId1" IS NOT NULL = 0`. |
| S-4 | **P1** | Soft delete is **convention only**: `BaseEntity.DeletedAt` (`BaseEntity.cs:10`), no global query filter, on the order of **230** hand-written `DeletedAt == null` predicates and **150–180** `IgnoreQueryFilters()` calls across `Application/ API/ Infrastructure/` *(amended: counted 229/162 as of `03093ad`; the two cross-reviews counted 231/178 and 233/148 with different grep scopes — the exact number drifts with every merge and is not load-bearing; do not cite it as a constant)*. Unique indexes that ignore it: `users(email, owner_id)` (`UserMapping.cs:26`), `roles(name, owner_id)` (`RoleMapping.cs:24`), `app_environments(name, owner_id)` (`AppEnvironmentMapping.cs:22`), `status_presentations(status_value, owner_id)` (`StatusPresentationMapping.cs:20`), `extension_sites(owner_id, origin)` (`ExtensionSiteMapping.cs:26`), `subscriptions(owner_id)` (`SubscriptionMapping.cs:24`), `role_tenant_overrides(role_id, owner_id)` (`RoleTenantOverrideMapping.cs:24`). Filtered precedents already exist: `projects.key` (`ProjectMapping.cs:28`), `project_app_urls` (`:33`), `project_builds` (`:32-34`), `api_keys` (`ApiKeyMapping.cs:45-48`), `workspace_settings` (`WorkspaceSettingMapping.cs:30`). | as cited | A soft-deleted user blocks that email in that workspace forever (unique violation → raw 500, exactly the bug class `20260831153128_MakeProjectKeyIndexFilterSoftDeleted` fixed for projects). Same for a re-added role/environment/status/extension origin. | **DB-05**: make the seven indexes partial on `deleted_at IS NULL` (and see S-5); *(amended)* plus `plans(name)`, `plans(slug)`, `app_settings(key)` — AGY 1.3 is right that `Plan` is soft-deleted (`PlanService.cs:132`), so a deleted plan burns its slug; `app_settings` has no delete path but is folded in for uniformity (10 indexes total). A global soft-delete query filter is **not** endorsed now — see §7 "not endorsed". |
| S-5 | **P1** | **Four unique indexes exist in production that the EF model does not know about**: `ix_projects_key_global`, `ix_status_presentations_status_value_global`, `ix_users_email_global` (`20260629130828_AddTenancy.cs:147-149`) and `ix_roles_name_global` (`20260629131255_AddTenantRoleNameIndex.cs:24`), created by raw SQL to cover the `owner_id IS NULL` case that a default (`NULLS DISTINCT`) unique index does not. They never appear in `AppDbContextModelSnapshot.cs`. `ix_projects_key_global` is now permanently empty (`projects.owner_id` is NOT NULL since `20260827130246`). | as cited | Snapshot drift: `dotnet ef migrations add` will never see, drop or recreate them; anyone reasoning from the model (including every execution doc so far) misses a constraint. Postgres 15 supports `NULLS NOT DISTINCT`, and the repo already uses it (`WorkspaceSettingMapping.cs:30`). | **DB-05**: replace each `(x, owner_id)` unique pair with one EF-declared partial unique index `… WHERE deleted_at IS NULL` + `AreNullsDistinct(false)`; drop the four raw indexes in the same migration. |
| S-6 | **P2** | **Integer "logical" foreign keys with no constraint**: `ai_rules.project_id` (`AiRuleMapping.cs:22`), `invites.role_id/project_id/plan_id` (`InviteMapping.cs:27-28,34`), `predefined_actions.project_id` (`PredefinedActionMapping.cs:27`), `predefined_action_suggestions.project_id` (`…SuggestionMapping.cs:24`), `quick_access_links.user_id(uuid)/project_id/invite_id` (`QuickAccessLinkMapping.cs:16-18`), `role_tenant_overrides.role_id` (`RoleTenantOverrideMapping.cs:21`), `usage_events.project_id` (`UsageEventMapping.cs:17`). FKs that do exist are listed in `SCHEMA.md`. | as cited; snapshot `HasOne` census | Deleting a project (hard, via tenant delete) orphans its actions, suggestions, rules and magic links; a typo'd id is accepted. | **DB-06**: add the FKs with the cascade behaviour stated per column, after an orphan pre-check query (provided). |
| S-7 | **P2** | **Dual user identity.** `users.id int` is the PK (`api_keys.user_id` FKs to it, `ApiKeyMapping.cs:33-36`) but every other reference is the `uuid public_id` with no FK: `comments.author_id`, `replies.author_id`, `notifications.user_id/actor_id`, `ai_rules.user_id`, `predefined_actions.user_id`, `quick_access_links.user_id`, `device_logins.user_id`, and every `created_by/updated_by/deleted_by`. Author names are resolved by a separate `PublicId IN (…)` lookup (`CommentService.cs:1103-1115`). | as cited | Cannot be constrained today without choosing one identity; a hard-deleted user leaves dangling author ids (by design for audit columns, accidental for the rest). | **Not now.** Record the rule (DB-RULES §Identity): new user references use `public_id`; after DB-03 lands, revisit adding `FOREIGN KEY (author_id) REFERENCES users(public_id) ON DELETE RESTRICT` for `comments`/`replies` only (open question Q5). |
| S-8 | **P2** | **Workspace attributes stored on the admin user row**: `is_demo`, `expires_at`, `demo_extended`, `demo_comment_cap_override`, `demo_ttl_hours_override`, `recipient_email` (`User.cs:43-56`) describe the *workspace*, not the person — visible in `TenantResponse` copying them straight off the user (`TenantService.cs:118-122`). | as cited | Ownership transfer (`UserService.cs:339`) moves the admin role but not these fields; a second admin can't see them. | **DB-03 stage B** (later, separate doc): move them to `workspaces`. Listed in DB-03 "out of scope" so nobody does it half-way. |
| S-9 | **P2** | **Retention is unimplemented**: `usage_events` (append-only, not a `BaseEntity`, `UsageEvent.cs:5-15`), `notifications`, `page_context_snapshots` (jsonb console/network bags, `PageContextSnapshotMapping.cs:31-43`) and expired `invites` grow without bound; `PlanEntitlements.RetentionDays` exists (`PlanEntitlements.cs:27`) but is only mapped to a DTO (`PlanService.cs:168,189`). Only `device_logins` is swept, inline on `StartAsync` (`DeviceLoginService.cs:47-56`), and demo tenants hourly. | as cited | Not a problem at today's scale; becomes the largest tables within months of real traffic and makes dumps/restores slow. | **DB-08**: one hosted retention job modelled on `DemoCleanupService`, batched `ExecuteDeleteAsync`. **Needs owner numbers** (periods) before implementation. |
| S-10 | **P2** | Legacy plaintext `users.api_key` column + unique index remain after the R1-06 hardening (`UserMapping.cs:37-38`, `User.cs:40`); the backfill nulls every value on boot (`API/Startup/ApiKeyBackfillService.cs:31-34,69`), and R1-06 scheduled the drop for R2 (`ApiKey.cs:10-12`). | as cited | Dead column on the most sensitive table; every reviewer re-asks whether keys are stored in plaintext. | **DB-07**: verify all NULL, drop column + index. |
| S-11 | **P3** | Three JSON-in-varchar columns on `projects`: `tech_stack` (2048), `ai_tools_used` (1024), `environment_selector_role_ids` (1024) (`ProjectMapping.cs:35-37`) — serialized by hand, not `jsonb`, no shape validation at the boundary the way `JsonColumn` provides. | as cited | Cannot be queried or indexed; length cap is the only guard. | Not endorsed now; convert to `jsonb` via `JsonColumn.ConfigureJsonColumn` the next time any of them is touched (rule recorded). |
| S-12 | **P3** | `projects.app_url` (single legacy URL, `Project.cs:24`) coexists with `project_app_urls`; the seeder back-fills one into the other on every boot (`API/Seed/AdminSeeder.cs:92-118`). | as cited | Two sources of truth for "where is this app". | Not endorsed now (still read by quick-access invites); revisit when R1-09's retirement of the `default` environment completes. |
| S-13 *(added 2026-09-22 evening)* | **P1** | **A workspace can lose its only Workspace Admin.** `UserService.UpdateAsync` lets a caller move a user to any non-admin role; the only guard is self-demotion (`UserService.cs:251-258`, `CannotChangeSelfFromAdmin`), so a **super admin can still set another tenant's sole Workspace Admin to `Client` today**, and on 2026-08-29 (`5a1136c`, `UserService.cs:231-249` at that commit) there was no guard at all. `DeleteAsync` does refuse (`:307`), `TransferOwnershipAsync` swaps roles atomically (`:339-382`) — the role-change path is the gap. Production evidence: owner id `cab219c2…` — one live user (role `Client`), one tenant role, one project, created 2026-08-29, **no admin**; invisible to `TenantService.ListAsync` (enumerates by role name, `:47`), unmanageable, and it would have tripped DB-03's abort. | as cited; DB-03a §3.5 | A tenant nobody can administer or even see; DB-03's model (every `owner_id` has an admin) is violated by a UI action. | **DB-03a** removes the instance (owner: "delete it"). Code fix recommended as a code-only doc (working title **DB-11**): in `UpdateAsync`, refuse to change the role of the tenant's *last* Workspace Admin for **any** caller (super admin included; use `TransferOwnershipAsync` or `HardDeleteAsync` instead), plus a `ListAsync`-side warning query for admin-less owner ids. |
| S-14 *(added 2026-09-22 evening)* | **P2** | Eleven write paths still stamp `TenantStamp.OwnerFor(_currentUser) ?? _currentUser.Id` (`ProjectService.cs:220`, `UserService.cs:114`, `PredefinedActionService.cs:53,93`, `CommentFieldService.cs:255,273`, `InviteService.cs:115,380`, `AiRuleService.cs:143`, `EntitlementService.cs:77`, plus `ProjectService.cs:298,763` variants). The fallback was "defensive" for pre-tenancy users with a null tenant claim; since `20260827130246` + `50583ac` such users no longer legitimately exist, so the fallback can only ever mint a **new** owner id equal to a person's `public_id` — a tenant with no admin (same shape as S-13). | as cited | Any future null-tenant JWT (bug, token from an old build) silently creates an orphan tenant instead of failing. After DB-03, the FK turns this into a hard `DbUpdateException` — good, but the message will not explain why. | Fold into **DB-11**: replace each `?? _currentUser.Id` with an explicit Forbidden/`InvalidOperation` result and a log line; one test per site with `TenantId = null, IsSuperAdmin = false`. |

---

## 4. Integrity and indexing

| # | Sev | Finding | Evidence | Fix |
|---|---|---|---|---|
| I-1 | P2 | `owner_id` is nullable in 15 of 23 tables although the service layer never writes NULL for most of them (`WorkspaceSettingMapping.cs:21-23`, `PredefinedActionSuggestionMapping.cs:23`, `PageContextSnapshotMapping.cs:27`, `ReplyMapping.cs:27`, `ProjectBuildMapping.cs:25`, `QuickAccessLinkMapping.cs:15`, `ApiKeyMapping.cs:24`, `NotificationMapping.cs:23`, `ExtensionSite` is already NOT NULL). Legitimately nullable: `users`, `roles`, `status_presentations`, `app_environments` (global bucket), `invites` (new-workspace invite), `device_logins` (pre-approval), `usage_events` (super-admin events). | as cited | Fold into **DB-03**: after the FK exists, tighten `replies`, `page_context_snapshots`, `project_builds`, `quick_access_links`, `predefined_action_suggestions`, `workspace_settings`, `api_keys`, `notifications` to NOT NULL **only after** the rehearsal query shows zero NULLs (listed in DB-03 §3). |
| I-2 | P2 | `notifications.project_id` is `Restrict` while `notifications.comment_id` is `Cascade` (`NotificationMapping.cs:33-34`) — inconsistent for a row that is pure derived data. | as cited | **DB-06** changes it to `Cascade` (a notification never outlives its project). |
| I-3 | P3 | Redundant single-column indexes whose column already leads a composite: `ai_rules(owner_id)` vs `(owner_id, project_id)` (`AiRuleMapping.cs:29-30`), `predefined_actions(owner_id)` vs `(owner_id, project_id)` (`PredefinedActionMapping.cs:36-37`). | as cited | Drop the single-column ones in **DB-06** (same migration, additive-safe). |
| I-4 | P3 | No index on `comments.author_id` / `replies.author_id`; unknown whether any hot path filters by author (the widget's "mine" flag is computed client-side, `CommentService.cs:1103`). | as cited | Open question Q6; do nothing until a query needs it. |
| I-5 | P3 | `usage_events.meta` is `jsonb` with `HasMaxLength(2000)` (`UsageEventMapping.cs:33-36`) — `HasMaxLength` is meaningless on `jsonb`; the cap is not enforced. | as cited | Note only; enforce at the validator if it matters. |
| I-6 | P3 | Hot-path indexes present and correct: `comments(project_id, created_at) WHERE deleted_at IS NULL`, `comments(owner_id, created_at) …` (`CommentMapping.cs:76,79`), `usage_events(owner_id, project_id, type, created_at)` (`UsageEventMapping.cs:42`), `notifications(user_id, read_at)` (`NotificationMapping.cs:37`), hash lookups on `api_keys.hash`, `quick_access_links.token_hash`, `device_logins.device_code_hash`, `invites.code`. Every FK has its EF-conventional index. | snapshot | None. |

---

## 5. Migration hygiene

| # | Sev | Finding | Evidence |
|---|---|---|---|
| M-1 | P1 | Schema + data mixed in single migrations is the house style: `ProjectPerEnvironmentActivation` (add → `UPDATE` → drop, `20260903191419:16-42`), `EnforceOwnerIdNotNull` (`UPDATE` → `SET NOT NULL`, `20260827130246:24-31`), `FixPayloadFlagsJsonDefault` (`AlterColumn` + repair `UPDATE`, `20260915163653:13-46`), `MigrateDefaultProjectAppUrlsToLocal` (data-only, empty `Down`). Acceptable at 104 rows; it is exactly what the rules must forbid before the first table passes ~1 M rows (an `UPDATE … WHERE` inside the migration transaction takes a full-table lock for its whole duration). | cited |
| M-2 | P1 | Snapshot drift: the four raw indexes (S-5) and the shadow `ProjectId1` (S-3). | cited |
| M-3 | P2 | Personal e-mail addresses and production tenant GUIDs are committed in migration **comments** (`20260827124245_ReassignPointerLandingOwnership.cs:16-17`, `20260827125323_ReassignRemainingSuperAdminOwnedResources.cs:20-21`). The GUIDs are load-bearing (the `UPDATE` targets them) and must stay; the e-mail comments are not part of the migration id and can be edited freely. | cited |
| M-4 | P2 | `Down()` methods are unreviewed: `MigrateDefaultProjectAppUrlsToLocal.Down` is empty (irreversible, unstated); `ProjectPerEnvironmentActivation.Down` re-adds `is_active` with `defaultValue: false` and then computes it — fine, but never tested. | cited |
| M-5 | P2 | The frozen-id table in `REBRANDING-PLAN.md` §4.1 lists two migrations; **all 58 are frozen by the same rule** (`__EFMigrationsHistory` PK). The rules point at §4.1 and say so. | `REBRANDING-PLAN.md` §4.1 |
| M-6 | P3 | Historical backfills are idempotent by construction (guarded `WHERE`, `IF NOT EXISTS`, `ON CONFLICT`-free but re-runnable): good precedent — keep it. `AdminSeeder` is the other idempotent backfill channel (`AdminSeeder.cs:39-118`, `171-298`), guarded by `LegacyBackfillCompleted` for the one-time subscription backfill (`:246-298`). | cited |

---

## 6. Evolution readiness — "can we add X without touching user data?"

| Change class | Safe pattern in this codebase | Precedent |
|---|---|---|
| New per-row attribute | Nullable column or `HasDefaultValue`/`HasDefaultValueSql`; one `AddColumn`; no backfill needed. | `20260917230734_AddCommentLanguage`, `20260917220722_AddReplyIsAi`, `20260912125359_AddProjectCaptureTextContent` |
| New structured bag on an existing row | `jsonb` via `JsonColumn.ConfigureJsonColumn(col, "'{}'"|"'[]'")` (tolerant parse, canonical write, comparer) — `Infrastructure/Mappings/JsonColumn.cs:67-75`. Owned `ToJson` only when the shape is fixed and queried as a unit (`Comment.Element`). Cap sizes at the validator; document the shape in the entity doc-comment. | R4-01 `comments.custom_fields`, `workspace_settings.comment_field_definitions` |
| New tenant-scoped entity | `: BaseEntity`, `OwnerId` (NOT NULL unless a global bucket is *designed*), mapping with explicit `HasColumnName`, query filter copied from the `Project` shape (`AppDbContext.cs:77`), `HasIndex(OwnerId)`, a `TenantQueryFilterTests`-style test proving tenant B sees nothing. After DB-03: FK `owner_id → workspaces(id)`, and the entity added to the `HardDeleteOrder` list (the reflection test fails otherwise). | `WorkspaceSetting` (R4-01 A2), `ProjectBuild` (`20260912193031`) |
| New relationship | Real FK with explicit `OnDelete`; EF adds the index. State the cascade in the mapping comment. | `ProjectBuildMapping.cs:27`, `PageContextSnapshotMapping.cs:45`, `CommentMapping.cs:48-49` (SetNull) |
| Rename column/table | **Never in one migration.** Expand (add new) → dual-write in code → backfill migration (batched) → switch reads → contract (drop) in a later release. Rebranding plan §11.1 already forbids renames of schema objects. | Anti-precedent: `ProjectPerEnvironmentActivation` did all three in one `Up()` |
| Split a table (e.g. workspace out of users) | Additive table + backfill + FKs first (DB-03 stage A); move columns later (stage B) with dual-read. | DB-03 |
| Type change / narrowing | New column of the new type + backfill + swap reads + drop old; widening (`WidenUsageEventSource`, `20260917234138`) is safe in place. | `20260917234138` |
| Enum value | Enums are stored as `int` (`CommentStatus`, `EnvironmentTag`, `NotificationType`, `SuggestionStatus`, `BuildSource`). Append values; **never renumber or reuse** an int. | `Domain/Enums/*` |
| Backfill | Separate migration or `AdminSeeder` step; guarded `WHERE`; for tables past ~100 k rows: batched by `id` range in a loop outside one transaction (see DB-RULES). | `20260915163653:44-46`, `AdminSeeder.cs:92-118` |

---

## 7. Recommendations → execution docs

Endorsed (each has a doc in `execution/`):

| Doc | Title | Class | Owner decision needed first? | Status 2026-09-22 *(amended)* |
|---|---|---|---|---|
| [DB-01](execution/DB-01-offsite-backup-and-restore-rehearsal.md) | Off-box backup copy, uploads volume in the backup, backup freshness gate (rehearsal done) | Ops (no schema) | **Yes** — provider (Q1: R2 / B2 / Oracle / Google Drive) for the off-box part only | ✔ **Deployed** 2026-09-22 ~10:15 UTC — Q1 answered (Oracle Object Storage, bucket `pointer-backups`, rclone remote `offsite`); both parts A and B shipped. Nothing left; production-side restore still unexercised (rehearsed locally only) |
| [DB-02](execution/DB-02-migration-safety-guard.md) | Guard test for destructive migrations; strip PII from migration comments | Additive (test only) | No | **Shipped** 94ebf27; hardening follow-up in its §11 |
| [DB-03](execution/DB-03-workspaces-table.md) | `workspaces` table (own `name`), `owner_id` FKs, complete tenant hard-delete, name reads moved off `users` | Expand (additive + abort-guarded backfill + FKs) | No — Q2 = **(b) abort**, Q3 = **own attribute, placeholder `Workspace`** (answered 2026-09-22) | ✔ **Deployed** 2026-09-22 12:05 UTC, batched with DB-06/DB-07/DB-03b(API)/DB-08 (`pre-db03-08`); 2 `workspaces` rows, both still the placeholder |
| [DB-03a](execution/DB-03a-orphan-owner-cleanup.md) *(new, evening)* | Orphan `owner_id` cleanup before DB-03: delete 15 dead demo fragments (S-2), reassign 40 `replies` `95b7f3ee → 98699076`, delete the admin-less tenant `cab219c2` (owner: "delete it"); abort-guarded, idempotent, before/after census | **Destructive** (data-only contract migration) | No — both approval lines filled in its header | ✔ **Deployed** 2026-09-22 10:48 UTC (`pre-db03a`); prod census 17→2 distinct owners, 15→0 orphans, 40→0 stray replies; 13 dead ids found (not the estimated 12–15) |
| [DB-03b](execution/DB-03b-workspace-name-surface.md) *(new)* | `GET /api/admin/workspace`, `PUT /api/admin/workspace/name`, `TenantResponse.WorkspaceName`, Settings card (dashboard tasks) | Code only | No | ✔ API half **deployed** 2026-09-22 12:05 UTC in the `pre-db03-08` batch; **dashboard card deployed** 2026-09-22 ~12:45 UTC (client `@moamen-ui/pointer-react` 1.0.40 published from prod, pointer-dashboard 713c1cc, `WorkspaceNameCard` in Settings + `workspaceName` in the tenants table) |
| [DB-04](execution/DB-04-drop-shadow-projectid1.md) | Fix `ProjectAppUrl` mapping, drop shadow `ProjectId1` | Contract (column always NULL) | No | ✔ **Shipped** 7a015cb; **deployed** 2026-09-22 08:18 UTC (`pre-db04`) |
| [DB-05](execution/DB-05-soft-delete-aware-unique-indexes.md) | Partial + `NULLS NOT DISTINCT` unique indexes (10); retire the 4 raw indexes | Expand/Contract (indexes only) | No | ✔ **Shipped** b35cf0c; **deployed** 2026-09-22 08:42 UTC (`pre-db05`; 10 `ux_*_live`, 0 `_global`) |
| [DB-06](execution/DB-06-foreign-keys-for-logical-references.md) | Real FKs for integer logical references; cascade fixes; drop 2 redundant indexes | Additive (constraints) | No — Q4 = **`SET NULL`** (answered 2026-09-22) | ✔ **Deployed** 2026-09-22 12:05 UTC in the `pre-db03-08` batch; 8 indexes created (not 10 — two absorbed by existing prefix indexes) |
| [DB-07](execution/DB-07-drop-users-api-key.md) | Drop `users.api_key` | **Destructive** (column) | No — **approved 2026-09-22 by Moamen** (line filled in its header) | ✔ **Deployed** 2026-09-22 12:05 UTC in the `pre-db03-08` batch via `POINTER_APPLY_CONTRACT=1`; `users.api_key` gone |
| [DB-08](execution/DB-08-retention-job.md) | Retention job for `usage_events` (180 d), read `notifications` (90 d), orphaned `page_context_snapshots` (30 d), dead unused `invites` (90 d) | Code + deletes | No — Q8 = **defaults stand, ships enabled** (answered 2026-09-22) | ✔ **Deployed** 2026-09-22 12:05 UTC in the `pre-db03-08` batch, enabled by default, `RETENTION_*` overridable |
| [DB-09](execution/DB-09-migration-apply-gate.md) | Mechanical R7 gate: `[ContractMigration]` + `Program.cs` refusal + `deploy-api.sh` explicit path | Additive (code + one compose env line) | No | ✔ **Merged** ff25a8d (711 tests); **deployed** 2026-09-22 ~10:12 UTC as an ordinary deploy, ahead of the batch (R7.1 point 7); gate types confirmed in the running binary |
| [DB-10](execution/DB-10-ci-postgres-migration-job.md) | CI job: apply all migrations to an empty `postgres:15`, snapshot check, Down/Up round-trip, guard tests | CI only | No (Q7 answered yes) | ✔ **Merged** 827cfd8; green on every push 2026-09-22 (runs 35706559115, 35717887752, 35721695261, 35724951372) |

**Recommended order (amended 2026-09-22 evening, after the orphan census):**
DB-02 ✔ → DB-04 ✔ (deployed) → DB-05 ✔ (deployed) → DB-10 ✔ (merged, CI green) → DB-09 ✔ (merged;
**deploy it now with an ordinary run**) → **DB-03a** (its own PR; its own contract deploy
`POINTER_APPLY_CONTRACT=1 POINTER_CONTRACT_LABEL=pre-db03a bash scripts/deploy-api.sh`; prod census
before/after pasted) → **DB-03 → DB-03b → DB-06 → DB-07 → DB-08** (implement back-to-back on
`main`, one PR each, in this order; DB-03 scaffolded only after DB-03a's migration file exists) →
**one contract deploy** `POINTER_APPLY_CONTRACT=1 POINTER_CONTRACT_LABEL=pre-db03-08 bash
scripts/deploy-api.sh` → dashboard half of DB-03b (client regen from prod, once) → **DB-11**
(code-only: S-13 last-admin guard + S-14 fallback removal; doc not yet written — ask the
db-architect for it after the batch) → DB-01 part B when Q1 lands (part A any time).

**Status 2026-09-22 (night): every step above has shipped** — DB-09 deployed ~10:12 UTC (ordinary
deploy), DB-03a deployed 10:48 UTC (`pre-db03a`), DB-03/DB-03b(API)/DB-06/DB-07/DB-08 deployed
12:05 UTC in one `pre-db03-08` contract deploy, DB-01 deployed ~10:15 UTC (both parts; Q1 answered
Oracle Object Storage), and DB-10 has been green on every push today. Only two things remain: the
**dashboard half of DB-03b** (client regen + `dashboard-agent`, still pending) and **DB-11** (its
doc is not yet written). DB-01 has nothing left.

**Batching verdict (asked by the caller): yes — one deploy for DB-03..08, but DB-03a goes
alone first** (R7.1 point 8: a row-level cleanup never rides with the structural migration that
depends on it). DB-03, DB-06, DB-07 carry
`[ContractMigration]` migrations; DB-03b and DB-08 are code only. With a single production
workspace, a restore that takes seconds, DB-10 applying the whole chain from empty on every PR, and
DB-09 enforcing the stop → dump → boot sequence, four separate stop/dump/boot cycles would add four
maintenance windows and no safety. The conditions and the rollback are written as
[**DB-RULES R7.1**](DB-RULES.md#r71-batching-several-contract-docs-into-one-deploy-added-2026-09-22-evening);
the non-negotiable parts: one PR per doc (never merge two docs' migrations into one file), **all**
pre-checks green on prod immediately before the run (DB-03 §9.1 `0 orphans`, DB-06 §3 ten zeros,
DB-07 §2 zero), one combined R11 rehearsal on a same-day dump **taken after DB-03a is in prod**, one
`pre-db03-08` dump, all-or-nothing rollback (restore + checkout the commit before DB-03), and the
DB-09 `deploy-api.sh` already on the VM before the run. Batching ends the day real tenants exist
(R7.1 point 1).

Why this order: DB-03 before DB-06 so the orphan pre-check has one root and DB-06's `usage_events`
FK matches DB-03's on the same table; DB-03b right after DB-03 so the API exposes the rename
endpoint in the same image the dashboard-agent regenerates from; DB-07 after DB-09 (first
Destructive migration under the gate — now satisfied); DB-08 last because it deletes and its first
pass runs 5 min after boot, i.e. after the batch's smoke check.
**Docs still needing an owner decision before implementation:** none — DB-03a's two approval
lines are filled ("aborting could be better while we need to clean the db"; "delete it"). DB-01
part B still waits for Q1 (provider choice — the doc already works for any of the four).

Not endorsed (one line each, so they are not re-raised):

- **Global EF soft-delete query filter** — 162 `IgnoreQueryFilters()` calls would silently start bypassing soft-delete too; the fix is bigger than the problem at this stage. Re-evaluate after DB-03 when the filter set is reworked.
- **Switching `BaseEntity.Id` to `uuid`/`bigint`** — `int` identity gives 2.1 B rows per table; the comments table would need ~60 years at 1 k/day. Not a launch risk.
- **Row-level security in Postgres** — the app enforces tenancy with a single-connection-role model; RLS would require `SET LOCAL` per request and a second rollout of every filter. Application filters + DB-03 FKs are enough for the current threat model.
- **Merging `workspace_settings` into `workspaces`** — keep the satellite; it already has a converter and tests. DB-03 gives it an FK instead.
- **Renaming any table/column for the rebrand** — rebranding plan §11.1 already forbids it; nothing in the schema carries the brand.
- **Converting `projects.tech_stack`/`ai_tools_used`/`environment_selector_role_ids` to `jsonb`** — only when the columns are next touched (S-11).
- **Indexing `author_id`** — no query needs it yet (I-4).
- *(amended)* **Treating the `PredefinedActionSuggestion` filter as a tenant leak (AGY 1.1)** — the `(TenantId == null && !strict && OwnerId == null)` branch is the codebase-wide legacy null-bucket branch (`AppDbContext.cs:11-17`), disabled in production by `Tenancy__StrictNullTenantIsolation=true` (`docker-compose.prod.yml:30`); null-owner suggestions are never written. Comment-accuracy nit at most.
- *(amended)* **Switching `project_app_urls.app_environment_id` from Cascade to Restrict now (AGY 1.2)** — the only delete path for environments is soft and in-use-guarded (`AppEnvironmentService.cs:113-119`); soft deletes fire no FK cascade. Recorded as a one-line watch item in DB-06 §10: switch to `Restrict` the day a hard-delete path for global environments appears.

---

## 8. Open questions for the owner

| # | Question | Blocks | Status 2026-09-22 *(amended)* |
|---|---|---|---|
| Q1 | Off-box destination. Narrowed 2026-09-22 to **Cloudflare R2, Backblaze B2, Oracle Object Storage (S3-compatible) or Google Drive** — DB-01 now has an "If Google Drive" variant (rclone `drive`, headless token or service account, `--drive-use-trash=false`); the scripts are identical for all four. | DB-01 (off-box part only) | **Answered: Oracle Object Storage** 2026-09-22 — bucket `pointer-backups`, rclone remote `offsite`. DB-01 part B deployed ~10:15 UTC alongside part A; nothing left in that doc. |
| Q2 | Orphan `owner_id` values (no Workspace Admin user, live or soft-deleted): attach as "Recovered" rows, or abort? | DB-03 | **Answered (b) abort** 2026-09-22 ("aborting could be better while we need to clean the db"). DB-03 §3.3 3b raises `DB-03 ABORT: … <ids>` inside the migration transaction; §9 step 1 is a mandatory prod pre-check whose output must be `0 orphans`. Soft-deleted-only admins still get a row (not an orphan). **The caller ran the diagnostic on a same-day prod dump: 16 of 17 owner ids are orphans today** (15 dead demo fragments — S-2 confirmed; `95b7f3ee` on 40 replies; the admin-less `cab219c2` — S-13) → [DB-03a](execution/DB-03a-orphan-owner-cleanup.md) cleans them before DB-03. |
| Q3 | Initial `workspaces.name`: the admin's display name, or its own attribute? | DB-03, DB-03b | **Answered: own attribute** 2026-09-22 ("the admin display name should be separated and not same as the workspace name"). Backfill seeds the constant placeholder `Workspace` (rationale DB-03 §3.1); every read of the name moves to `workspaces.name` (DB-03 §3.5); the admin sets the real name via DB-03b. |
| Q4 | `usage_events.project_id`: FK with `ON DELETE SET NULL`, or unconstrained? | DB-06 | **Answered: `SET NULL`** 2026-09-22 ("choose the best for clean db"). No approval line needed (additive constraint). |
| Q5 | After DB-03: constrain `comments.author_id`/`replies.author_id` to `users(public_id)`? It would make deleting a *user* while keeping their comments impossible (today users are only hard-deleted with their whole workspace). | future doc | **Open**, not blocking |
| Q6 | Is there, or will there be, a "comments by author" query (widget "mine" filter server-side, dashboard per-person view)? Decides `comments(author_id)` index. | I-4 | **Open**, not blocking |
| Q7 | Should CI get a `postgres:15` service job that runs `dotnet ef database update` from empty plus the DB-02 guard? | P0-5 | **Answered yes** (caller, 2026-09-22) → [DB-10](execution/DB-10-ci-postgres-migration-job.md) specifies the YAML verbatim; the implementer, not this reviewer, edits `.github/workflows/` |
| Q8 | Retention periods for DB-08. | DB-08 | **Answered: defaults stand** 2026-09-22 — `usage_events` 180 d, read `notifications` 90 d, orphaned `page_context_snapshots` 30 d, never-used expired/revoked `invites` 90 d; **ships enabled**, overridable via `RETENTION_*` in `.env.prod`. |
| Q9 | `projects.app_url` — still needed once every project has a `project_app_urls` row? | S-12 | **Open**, not blocking |

---

## 9. Cross-review adjudication (2026-09-22)

Two independent reviews of this document and the execution docs were commissioned the same day.
Verdicts below were checked against the tree by the caller and by this reviewer; where a finding
is rejected the evidence is cited so it is not re-raised.

| Finding | Claim | Verdict | Why / where it landed |
|---|---|---|---|
| AGY 1.1 | `PredefinedActionSuggestion` filter leaks across tenants (Blocker) | **Rejected** | Legacy null-bucket branch shared by ~20 entities, strict in prod (`docker-compose.prod.yml:30`); no cross-tenant path. Listed under "not endorsed". |
| AGY 1.2 / GLM B7 | `project_app_urls.app_environment_id` Cascade wipes every tenant's URLs (Blocker) | **Downgraded to P3 watch item** | Unreachable today: environments are only soft-deleted, in-use-guarded (`AppEnvironmentService.cs:113-119`). One line added to DB-06 §10. |
| AGY 1.3 | `plans`/`app_settings` unique indexes wrongly excluded from DB-05 (Major) | **Accepted** | `PlanService.cs:132` soft-deletes plans. Folded into DB-05 (10 indexes). |
| AGY 1.4 / GLM B2 | DB-02 marker does not stop `MigrateAsync`; R7 unenforced (Blocker / Major) | **Accepted** (the "falsely claims" framing rejected — DB-02's stated goal was the marker) | New **DB-09**; DB-RULES R7 now points at it. |
| AGY 2.1 / GLM A3 | S-4 counts do not reproduce | **Accepted** | Counts removed as constants (S-4 amended). |
| AGY 3.1 | DB-03 `HardDeleteOrder` loop contradicts the generic helper | **Accepted** | DB-03 §3.4: 22 explicit `await DeleteOwnedAsync<T>()` calls; the array feeds the reflection test only. |
| AGY 3.2 / GLM C1 | DB-RULES correct for PG15 + EF8; two completions for R4 | **Accepted** | R4 amended (INVALID index after a failed `CONCURRENTLY`; `NOT VALID` FKs must be validated). |
| GLM A1 | "Restore never executed" is stale | **Accepted** | P0-1 amended; DB-01 re-scoped. |
| GLM A2 | 59 migrations; `SCHEMA.md` still lists `ProjectId1` | **Accepted** | Snapshot row, `SCHEMA.md`, DB-02 §7 amended. |
| GLM A4 | DB-02 guard skips files without `void Down(`; regex misses `DropCheckConstraint`/`DropSequence`/`AlterDatabase` | **Accepted** | DB-02 §11 hardening tasks. |
| GLM B1 | DB-03 expected-index list wrong; stop-rule misfires | **Accepted** (census re-verified: 5 definite + 3 optional) | DB-03 §3.2 / §7 amended. |
| GLM B3 | DB-01 has no failure/freshness signal | **Accepted** | DB-01: `deploy-api.sh` refuses when the newest dump is older than 26 h; healthcheck ping; weekly `rclone ls`. |
| GLM B4 | DB-03 Migration-1 recipe hand-moves generated operations | **Accepted** | Replaced by the comment-out two-scaffold recipe. |
| GLM B5 | DB-03 3a misses soft-deleted founding admins | **Accepted** | Sentence + query in DB-03 §3.3; Q2 row notes it. |
| GLM B6 | DB-04 marker names an agent; `Down()` omits `Restrict` | **Noted, no action** | File is frozen; the marker cites the standing owner instruction, which DB-RULES R7 now explicitly allows. `NO ACTION` ≡ `RESTRICT` for non-deferred constraints. |
| GLM B8 | DB-01 uploads tar has no consistency/size story | **Accepted** | One sentence in DB-01 §3. |
| GLM D1 | Drop DB-04 from the queue; pull the CI job forward | **Accepted** | Order amended; **DB-10** created. |
| AGY §4 "DB-01 safe as-is" | — | **Superseded** by GLM A1/B3/B8 | DB-01 needed re-scoping. |
