# DB-18 code review — 2026-09-24 (branch feat/db-18-api)

## Gemini 3.8 Flash (agy)

| severity | finding | evidence file:line | suggested fix |
|---|---|---|---|
| BLOCKER | Anonymous and background paths omit `.IgnoreQueryFilters()` on tracked workspace lookups. In anonymous endpoints (`ConfirmDeletionAsync`, `PauseInsteadAsync`, `SpendLinkAsync`) and background jobs (`SendDueRemindersAsync`), `currentUser.TenantId` is null and `currentUser.IsSuperAdmin` is false, so `_unitOfWork.Workspaces.FirstOrDefaultAsync(...)` evaluates the global query filter to false and always returns null. `ConfirmDeletionAsync` and `PauseInsteadAsync` always return 409 Conflict (`AlreadyDeleted`), breaking deletion confirmation and pause-instead in production. `SpendLinkAsync` returns early without clearing `DeletionRequestedAt/By`, preventing links from being spent on failed attempts. `SendDueRemindersAsync` returns early without stamping `DeletionReminderSentAt = now;`, causing `ExecuteDueDeletionAsync` to permanently skip scheduled deletions for grace periods ≥ 48h. | [`WorkspaceLifecycleService.cs:635`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L635)<br>[`WorkspaceLifecycleService.cs:716`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L716)<br>[`WorkspaceLifecycleService.cs:758`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L758)<br>[`WorkspaceLifecycleService.cs:1035`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L1035) | Add `.IgnoreQueryFilters()` to each `_unitOfWork.Workspaces.FirstOrDefaultAsync(...)` query in `ConfirmDeletionAsync`, `SpendLinkAsync`, `PauseInsteadAsync`, and `SendDueRemindersAsync`. |
| BLOCKER | `TenantService.HardDeleteAsync` violates foreign key constraint `fk_users_workspaces_owner_id` on soft-deleted or GDPR-erased users. Line 672 filters `usersCreatedHere` with `u.OwnerId == workspaceId && u.DeletedAt == null`. Any user originating in the workspace who was soft-deleted or GDPR-erased (`IdentityEraseService.cs:338` sets `identity.DeletedAt = now`) is excluded from `usersCreatedHere` and is neither removed nor re-homed. When `_unitOfWork.Workspaces.Remove(workspace)` runs at line 717, Postgres aborts the transaction with a 23001 foreign key violation because `UserMapping.cs:54` specifies `OnDelete(DeleteBehavior.Restrict)`. | [`TenantService.cs:672`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/TenantService.cs#L672)<br>[`TenantService.cs:717`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/TenantService.cs#L717)<br>[`UserMapping.cs:54`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Infrastructure/Mappings/UserMapping.cs#L54) | In `TenantService.HardDeleteAsync`, load all users where `u.OwnerId == workspaceId` without filtering on `u.DeletedAt == null`, and remove (or re-home) every identity originating in the workspace before deleting the workspace row. |
| HIGH | Confirmation link password lockout threshold mismatch and coupling to general login failures. DB-18 §3.4 and §12 mandate spending the link after the 5th failed password attempt. Line 605 calls `await _lockout.IsLockedAsync(identity.Email)`, which queries `LoginAttemptLimiter` using `LoginLockout:Threshold` from `appsettings.json` (configured to 10, not 5). Furthermore, it shares the global `identity.Email` key, so prior failed dashboard logins prematurely burn the deletion link, while an attacker on an untouched account gets 9 attempts before lockout. `Db18WorkspaceLifecycleTests.cs:1077` masked this by injecting a private `LoginLockoutOptions { Threshold = 5 }`. | [`WorkspaceLifecycleService.cs:605`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L605)<br>[`LoginAttemptLimiter.cs:31`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Infrastructure/Auth/LoginAttemptLimiter.cs#L31)<br>[`appsettings.json:28`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/appsettings.json#L28)<br>[`Db18WorkspaceLifecycleTests.cs:1077`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Tests/Db18WorkspaceLifecycleTests.cs#L1077) | Track failed deletion confirmation attempts using a flow-specific cache key (e.g. `ws-del-fail:{workspace.Id}:{identity.Id}`) with a fixed 5-attempt limit before invoking `SpendLinkAsync(workspace.Id)`. |
| HIGH | `UsersController.Update` returns 400 Bad Request instead of 403 Forbidden for access grants on frozen workspaces. While frozen, `UserService.UpdateAsync` correctly returns `Result<UserResponse>.Forbidden(MessageKeys.Workspace.FrozenNoAccessGrant)` if a request specifies `Password`, `IsActive=true`, or an admin-tier `RoleId`. However, `UsersController.Update` only checks `result.IsNotFound` and `result.IsConflict`, falling through to `BadRequest(result)`. This returns HTTP 400 instead of HTTP 403 Forbidden, violating DB-18 §3.5 and §6 9a, and omitting `[ProducesResponseType(StatusCodes.Status403Forbidden)]`. | [`UsersController.cs:77-81`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Controllers/Admin/UsersController.cs#L77-L81)<br>[`UserService.cs:424`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/UserService.cs#L424) | Add `if (result.IsForbidden) return StatusCode(StatusCodes.Status403Forbidden, result);` and OpenAPI response attribute `[ProducesResponseType(typeof(Result), StatusCodes.Status403Forbidden)]` to `UsersController.Update`. |
| HIGH | `AuthController.WorkspaceDeletionConfirm` and `WorkspaceDeletionPauseInstead` return 400 instead of 409 on conflict. `ConfirmDeletionAsync` and `PauseInsteadAsync` return `Result.Conflict(MessageKeys.Workspace.AlreadyDeleted)` or `Result.Conflict(MessageKeys.Workspace.StateChanged)`. Neither action method inspects `result.IsConflict`, returning `BadRequest(result)` (HTTP 400) on concurrency/deleted conflicts instead of HTTP 409 Conflict. | [`AuthController.cs:251`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Controllers/AuthController.cs#L251)<br>[`AuthController.cs:265`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Controllers/AuthController.cs#L265) | Add `if (result.IsConflict) return Conflict(result);` and `[ProducesResponseType(typeof(Result), StatusCodes.Status409Conflict)]` to both endpoints in `AuthController`. |
| HIGH | Mandatory test implementations from DB-18 §6 9a omitted from test suite. Required tests `Job_And_Reminder_RunWithNoTenantCurrentUser`, `Freeze_AllowsRevocations_RefusesGrants`, `Preview_AccountsCount_EqualsRowsActuallyDeleted`, `Confirm_Concurrent_OnlyOneSchedules`, `PauseInstead_RacingConfirm_NoConstraintViolation`, `Cancel_WhileJobHoldsLock_Conflict`, `Reminder_RacingCancel_NoConstraintViolation`, `Reminder_Missed_ReschedulesPlus24h_Audited`, and `Reminder_GraceUnder2Days_NotSent` are absent. Existing confirmation tests unrealistically passed `AdminCaller(ws)` to `BuildService`, which masked the anonymous query filter blocker. | [`Db18WorkspaceLifecycleTests.cs:694`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Tests/Db18WorkspaceLifecycleTests.cs#L694)<br>[`Db18WorkspaceLifecycleTests.cs`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Tests/Db18WorkspaceLifecycleTests.cs) | Implement all omitted tests specified in §6 9a, and verify anonymous endpoints and background jobs execute with an unauthenticated/no-tenant context (`new FakeCurrentUser()`). |
| HIGH | `WorkspaceDeletionService.SweepOnceAsync` constantly resets failure count on skipped deletions. When `ExecuteDueDeletionAsync` returns failure (e.g. skipped because reminder is not yet sent or workspace is held by operator pause), line 146 calls `FailureCounts.TryRemove(id, out _)`. Because line 99 queries all workspaces where `DeletionScheduledFor <= now && !PausedByOperator` without checking reminder state, due workspaces with pending reminders are re-queried every 15 minutes, failing each time and clearing `FailureCounts`, preventing `LogCritical` from ever firing after 3 failures. | [`WorkspaceDeletionService.cs:146`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Hosted/WorkspaceDeletionService.cs#L146)<br>[`WorkspaceDeletionService.cs:99-109`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Hosted/WorkspaceDeletionService.cs#L99-L109) | In `dueIds` query, include reminder criteria `&& (w.DeletionReminderSentAt != null || w.DeletionScheduledFor - w.DeletionConfirmedAt < TimeSpan.FromHours(48))`, and only call `FailureCounts.TryRemove` when `result.IsSuccess` or when explicitly cancelled. |
| MEDIUM | `OperatorPauseAsync` and `OperatorResumeAsync` lack transactions and concurrency exception handling. Unlike `CancelDeletionAsync` and `OperatorCancelDeletionAsync`, `OperatorPauseAsync` and `OperatorResumeAsync` do not wrap execution in `ExecuteInTransactionAsync`, do not acquire `SELECT FOR UPDATE`, and do not catch `DbUpdateConcurrencyException`. If a concurrent deletion or update races these actions, an unhandled 500 error is thrown. | [`WorkspaceLifecycleService.cs:835`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L835)<br>[`WorkspaceLifecycleService.cs:871`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L871) | Wrap `OperatorPauseAsync` and `OperatorResumeAsync` in `_unitOfWork.ExecuteInTransactionAsync`, acquire `SELECT id FROM workspaces WHERE id = {0} FOR UPDATE`, and catch `DbUpdateConcurrencyException` mapping to `Conflict(MessageKeys.Workspace.StateChanged)`. |
| MEDIUM | `TenantService.StillDueAsync` omits `w.DeletedAt == null` for `owner_requested`. In `HardDeleteAsync`, `StillDueAsync()` for `owner_requested` checks `w.Id == workspaceId && w.DeletionScheduledFor != null && w.DeletionScheduledFor <= DateTime.UtcNow && !w.PausedByOperator`, but omits `&& w.DeletedAt == null`, unlike `ExecuteDueDeletionAsync` and all other lifecycle methods. | [`TenantService.cs:557-564`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/TenantService.cs#L557-L564) | Add `&& w.DeletedAt == null` to the `owner_requested` match branch of `StillDueAsync()`. |
| MEDIUM | Inconsistent omission of `.IgnoreQueryFilters()` on tracked loads in `CancelDeletionAsync` and `OperatorCancelDeletionAsync`. `fresh` is queried with `.IgnoreQueryFilters()`, but the subsequent tracked load `var tracked = await _unitOfWork.Workspaces.FirstOrDefaultAsync(...)` omits `.IgnoreQueryFilters()`. While current callers happen to satisfy the filter, this violates the DB-18 §3.4 rule that all lifecycle queries use `IgnoreQueryFilters()` with explicit predicates. | [`WorkspaceLifecycleService.cs:382`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L382)<br>[`WorkspaceLifecycleService.cs:931`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Services/Implementation/WorkspaceLifecycleService.cs#L931) | Add `.IgnoreQueryFilters()` to both `_unitOfWork.Workspaces.FirstOrDefaultAsync(...)` invocations. |
| MEDIUM | CLI `applyCommand` exits with code 1 instead of 2 on unhandled 423 from downstream API calls. `apply.ts` checks `exitIfWorkspaceFrozen` at startup (exiting with code 2). However, if that preflight check is skipped due to network glitch on `/api/auth/me` or if the workspace is frozen between `/me` and subsequent calls, downstream calls like `fetchQueue`, `runApply`, `markApplied`, or `markFailed` reject with `ApiError` 423. The unhandled rejection bubbles up to the CLI process error handler and exits with code 1 instead of code 2. | [`apply.ts:27-38`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/cli/src/commands/apply.ts#L27-L38)<br>[`apply.ts:308-318`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/cli/src/commands/apply.ts#L308-L318) | Catch `ApiError` with `err.code === 423` around queue fetching and mutation calls in `apply.ts`, log the workspace paused/deletion notice, and call `process.exit(2)`. |
| LOW | `WorkspaceFrozenFilter` attribute reflection ignores inherited attributes and endpoint metadata. Lines 69 and 74 call `GetCustomAttributes(typeof(AllowWhenWorkspacePausedAttribute), false)`. Passing `false` ignores attributes on inherited controller actions or base classes. It also bypasses standard ASP.NET Core endpoint metadata inspection. | [`WorkspaceFrozenFilter.cs:69-74`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Auth/WorkspaceFrozenFilter.cs#L69-L74) | Use `ctx.ActionDescriptor.EndpointMetadata.OfType<AllowWhenWorkspacePausedAttribute>().LastOrDefault()` or pass `inherit: true`. |
| LOW | `TenantsController` lacks `result.IsConflict` handling for `OperatorPause` and `OperatorResume`. Unlike `OperatorCancelDeletion`, `OperatorPause` and `OperatorResume` do not check `result.IsConflict`, falling through to `BadRequest(result)`. | [`TenantsController.cs:132`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Controllers/Admin/TenantsController.cs#L132)<br>[`TenantsController.cs:145`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/API/Controllers/Admin/TenantsController.cs#L145) | Add `if (result.IsConflict) return Conflict(result);` and `[ProducesResponseType(typeof(Result), StatusCodes.Status409Conflict)]` to both actions. |
| LOW | Missing `ck_workspaces_deletion_reminder_consistent` DB check constraint. Check constraints exist for pause, deletion request, and deletion schedule consistency, but none ensures `deletion_reminder_sent_at` can only be non-null when `deletion_scheduled_for IS NOT NULL`. | [`WorkspaceMapping.cs:17-30`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Infrastructure/Mappings/WorkspaceMapping.cs#L17-L30)<br>[`20260923220301_AddWorkspacesPauseAndDeletionState.cs:81-99`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Infrastructure/Migrations/20260923220301_AddWorkspacesPauseAndDeletionState.cs#L81-L99) | Add check constraint `(deletion_reminder_sent_at IS NULL) OR (deletion_scheduled_for IS NOT NULL)` in `WorkspaceMapping.cs`. |
| NIT | Missing DB-RULES R16 amendment and R19 text in `docs/db/DB-RULES.md`. DB-18 §5 task 19 specifies adding the R16 amendment (state-based single-use and danger rate limit) and DB-RULES R19 into `docs/db/DB-RULES.md`, but `git diff main...HEAD -- docs/db/DB-RULES.md` is empty. | [`DB-RULES.md`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/docs/db/DB-RULES.md)<br>[`DB-18-workspace-self-service-pause-and-delete.md:527-532`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/docs/db/execution/DB-18-workspace-self-service-pause-and-delete.md#L527-L532) | Add the R16 amendment and R19 text to `docs/db/DB-RULES.md`. |
| NIT | Redundant `TenantStamp.TryRequireOwner` check in `WorkspaceLifecycleGuard.CanManageAsync`. Line 29 calls `TenantStamp.TryRequireOwner(currentUser, out var ws)`, which verifies the tenant claim and checks `!currentUser.IsSuperAdmin`. Line 27 already checked `currentUser.IsSuperAdmin`. | [`WorkspaceLifecycleGuard.cs:29`](file:///Users/momen/Desktop/REPOS/pointer-db18-api/Application/Common/WorkspaceLifecycleGuard.cs#L29) | Simplify to `if (currentUser.TenantId != workspace.Id) return false;`. |

## Opus (code-reviewer)

| severity | finding | evidence file:line | suggested fix |
|---|---|---|---|
| BLOCKER | Anonymous confirm/pause-instead tracked re-load inside the lock uses `_unitOfWork.Workspaces` with the tenant filter on → no row without a tenant claim → Conflict(AlreadyDeleted) → 400 for every real user | WorkspaceLifecycleService.cs:635, :758; AppDbContext.cs:328-332; HttpCurrentUser.cs:26-27 | `IgnoreQueryFilters().FirstOrDefaultAsync(w => w.Id == ws && w.DeletedAt == null)` (tracked) |
| BLOCKER | Hosted reminder never stamps (no tenant → tracked load null → early return); ExecuteDueDeletionAsync requires DeletionReminderSentAt or grace < 48h → with 7-day grace nothing is ever deleted, no E3; logs "skipped (cancelled)" silently | WorkspaceLifecycleService.cs:1035, :1104-1108; WorkspaceDeletionService.cs:158-163 | IgnoreQueryFilters + explicit predicate |
| BLOCKER | SpendLinkAsync same filter bug → link never spent after lockout (D18.5 unenforced) | WorkspaceLifecycleService.cs:716-720 | IgnoreQueryFilters + explicit Id/DeletedAt |
| HIGH | Tests hide all three: every anonymous/job call uses a tenant-scoped AdminCaller or super admin; §6 9a `Job_And_Reminder_RunWithNoTenantCurrentUser` absent; no reminder tests | Db18WorkspaceLifecycleTests.cs:689-697, :1383-1420 | Run confirm, pause-instead, preview, SpendLink, SendDueReminders, ExecuteDueDeletion with `new FakeCurrentUser()`; add Reminder_* tests |
| MEDIUM | SpendLinkAsync no state re-check under lock: a correct confirm in another tab between validation and 5th wrong password → clears requested_at on a scheduled row → ck_workspaces_deletion_schedule_consistent violated, DbUpdateException uncaught → 500; can void another admin's newer request | WorkspaceLifecycleService.cs:706-730 | Pass validated requestedAt/requestedBy; spend only if DeletionScheduledFor == null && both match; catch DbUpdateException |
| MEDIUM | Link spent when the shared login lockout trips (prod threshold 10, shared with login failures), not on the 5th wrong password on this link | WorkspaceLifecycleService.cs:597-606; appsettings.json:28 | Keep lockout call + per-link IMemoryCache counter `delws:{ws:N}:{requestedAtMs}` 30-min TTL; spend at 5 |
| MEDIUM | Job treats every InvalidOperationException as race skip and resets failure counter → real faults never reach Critical | WorkspaceDeletionService.cs:166-175; TenantService.cs locked re-check | Dedicated `DeletionPreconditionChangedException : InvalidOperationException`; catch only that |
| LOW | Re-validation inside lock reads stale identity/membership (tracking queries, identity resolution) → password change/demotion in the window missed | WorkspaceLifecycleService.cs:628, :751; MembershipService.cs:31-51 | ClearChangeTracker() before re-validate, or AsNoTracking reads |
| LOW | Confirm checks PausedByOperator only on the pre-lock snapshot | WorkspaceLifecycleService.cs:572-575 vs :628-653 | Under lock: if tracked.PausedByOperator → Forbidden |
| LOW | Admin Pause/Resume write without lock/re-check → can overwrite/undo an operator pause | WorkspaceLifecycleService.cs:157-175, :198-216 | Locked txn; re-check PausedAt, PausedByOperator, DeletionScheduledFor |
| LOW | Operator-paused workspace in last 24h still gets E3; after operator resume a past-due schedule deletes next sweep with no fresh notice | WorkspaceLifecycleService.cs:987-1001, :851-872 | Exclude PausedByOperator from reminders; OperatorResume: if DeletionScheduledFor <= now+24h → now+24h, clear reminder, audit deletion_rescheduled |
| LOW | BCrypt.Verify throws SaltParseException on empty/non-bcrypt hash → 500 | WorkspaceLifecycleService.cs:597-600; BcryptPasswordHasher.cs:9 | try/catch + IsNullOrEmpty guard |
| LOW | Grant guard refuses no-op fields (IsActive=true on active member; Password == "") | UserService.cs:419 | `!IsNullOrEmpty(Password) || (IsActive==true && !membership.IsActive) || (role.GrantsAdmin && role.Id != membership.RoleId)` |
| LOW | Grant guard misses QuickAccess → full-member upgrade | UserService.cs:419 | Also refuse wasQuickAccess && !role.QuickAccess (or document) |
| LOW | `POST users/{id}/reject` removes access but is refused while frozen | UsersController.cs:46 | [AllowWhenWorkspacePaused] + coverage test |
| LOW | Widget add-comment shortcut still starts picking while paused | element.ts:1696 | `if (this._paused) return;` in startPicking() |
| LOW | Widget first-upload 423 shows two toasts | element.ts:1831, :2197-2200 | `if (!url && !this._paused)` toast |
| LOW | CLI: /me pre-check failure or mid-run freeze → ApiError(423) → generic exit, `--mark` may have committed locally | apply.ts:20-38; mark.ts:60-68; queue.ts:137 | Map 423 → exit 2 in apply's top-level catch; fetchQueue before commitAll in `--mark all` |
| NIT | "held by operator pause" log unreachable (due query already excludes PausedByOperator) | WorkspaceDeletionService.cs:102-107 | Drop the filter from the due query so held rows log "held", or log held ids separately |
| NIT | `pointer deployed` says "paused" when scheduled for deletion | deployed.ts:64, :89 | Print server message |
| NIT | NormalizeName uses literal invisible chars | WorkspaceLifecycleService.cs:138 | Use \u escapes |
| NIT | Paused notice shown as 'error' toast | element.ts:514 | Default/info kind |
| HIGH (tests) | Sqlite race tests §6 9a missing: Confirm_Concurrent_OnlyOneSchedules, PauseInstead_RacingConfirm_NoConstraintViolation, Cancel_WhileJobHoldsLock_Conflict, Reminder_RacingCancel_NoConstraintViolation, Job_CancelledBetweenQueryAndDelete_Skipped_NoFileDelete (InMemory ignores FOR UPDATE/txn/check constraints) | Db18WorkspaceLifecycleTests.cs:155 | Copy Db17HardDeleteAndCleanupTests Sqlite harness + ExtendingOnLockUnitOfWork |
| HIGH (tests) | Preview_AccountsCount_EqualsRowsActuallyDeleted, HardDelete_OwnerRequested_NotScheduled_Refused_NoAudit_NoFiles, HardDelete_OwnerRequested_OperatorPaused_Refused, Job_DueDeletion_HardDeletes_… missing | Tests/ | Add on Sqlite harness |
| MEDIUM (tests) | Freeze_AllowsRevocations_RefusesGrants, InviteAccept_FrozenWorkspace_Refused, Register_FrozenWorkspace_Refused (+ReapplyAfterRejection), WidgetStatus_Frozen_ActiveTrue_PausedTrue, LoginWithKey_ResponseCarriesFreezeFields, KeySession_EventsSummary_Returns423, DangerLimiter_PartitionsByIdentity, StateService_SoftDeletedWorkspace_NotFrozen, ConvertedDemoInVerifyWindow_CanPause, Confirm_AfterLeave_LinkInvalid, WorkspaceResponse_TenantB_SeesNoStateOfA missing | Tests/ | Add |

## Adjudication (fix pass) — 2026-09-24, worktree `pointer-db18-api` branch `feat/db-18-api`

Every row re-checked directly against the code (not against the finding's prose) before its verdict. Commits: `eecb6cc` (backend fix), `26c1360`
(cli fix), `cdbe41b` (widget fix), `893fe26` (docs), `bbe68b1` (backend tests), `7f1d8e7` (cli tests), `f1f896e` (widget tests).

### Gemini 3.8 Flash

| # | Row | Verdict | Commit |
|---|---|---|---|
| G1 | BLOCKER — anonymous/job `.IgnoreQueryFilters()` omitted | **Fixed** — verified: with no tenant claim and not super admin, the strict-own `Workspace` filter (`AppDbContext.cs`) evaluates false, so the plain `_unitOfWork.Workspaces.FirstOrDefaultAsync` in `ConfirmDeletionAsync`, `SpendLinkAsync`, `PauseInsteadAsync`, `SendDueRemindersAsync` always returned null. Added `IgnoreQueryFilters()` + explicit `Id == x && DeletedAt == null` to all four (and, for consistency, to `CancelDeletionAsync`/`OperatorCancelDeletionAsync`'s tracked loads, which happened to work today only because their callers carry a matching tenant claim / are super admin). Proved with `new FakeCurrentUser()` (no tenant, not super admin) tests for Confirm/PauseInstead/SpendLink/SendDueReminders — confirmed FAILED before the fix by temporarily reverting the four call sites and re-running (`Confirm_NoTenantCurrentUser_Succeeds`, `PauseInstead_NoTenantCurrentUser_Succeeds`, `Confirm_FiveWrongPasswords_NoTenantCurrentUser_LinkSpent`, `SendDueRemindersAsync_NoTenantCurrentUser_StampsReminder_AndMails` all failed with exactly the predicted symptom, then passed after restoring). `Preview_NoTenantCurrentUser_Succeeds` and `ExecuteDueDeletionAsync_NoTenantCurrentUser_HardDeletes` were added too but stayed green through the same revert — neither method actually had the bug (their own reads already used `IgnoreQueryFilters()`). | `eecb6cc`, `bbe68b1` |
| G2 | BLOCKER — `HardDeleteAsync` FK violation on soft-deleted/erased identities | **Fixed** — verified against real FK semantics (Sqlite, not reasoning alone): a new test seeding an already-soft-deleted identity with `OwnerId == workspaceId` reproduced `SQLite Error 19: FOREIGN KEY constraint failed` (the same class as Postgres 23503 on `fk_users_workspaces_owner_id`) before the fix, confirmed by temporarily reverting. Widened `usersCreatedHere` and the shared `IdentitiesDeletedWithWorkspace` query to drop `&& u.DeletedAt == null` — every identity whose legacy `owner_id` still points at the workspace is now resolved (deleted or re-homed), regardless of soft-delete state. See §12 adjudication note below on scope (the FK from a DB-11a same-e-mail-merge predecessor row is a separate, deeper, out-of-scope latent issue — not touched). | `eecb6cc`, `bbe68b1` |
| G3 | HIGH — lockout threshold mismatch / coupling to login failures | **Fixed** — added a dedicated per-link `IMemoryCache` counter (`delws:{ws:N}:{requestedAtMs}`, 30-min TTL) that spends the link on its own 5th wrong password; the shared `ILoginAttemptLimiter` call is kept alongside it (still a valid account-level defense) but no longer decides the spend. New test `Confirm_LinkSpentOnOwnFifthFailure_EvenWhenAccountLockoutThresholdIsHigher` uses a prod-shaped threshold of 10 and proves the link dies at attempt 5 while the account itself is still unlocked. | `eecb6cc`, `bbe68b1` |
| G4 | HIGH — `UsersController.Update` 400 instead of 403 | **Fixed** — added `IsForbidden` check + `[ProducesResponseType(..., 403)]`. | `eecb6cc` |
| G5 | HIGH — `AuthController` confirm/pause-instead 400 instead of 409 | **Fixed** — both actions now check `IsConflict` + `[ProducesResponseType(..., 409)]`. | `eecb6cc` |
| G6 | HIGH — §6 9a tests omitted, existing tests masked the blocker with `AdminCaller(ws)` | **Fixed in `d162874`** — the previously-not-implemented subset (`Freeze_AllowsRevocations_RefusesGrants`, `InviteAccept_FrozenWorkspace_Refused`, `Register_FrozenWorkspace_Refused` + `Register_ReapplyAfterRejection_FrozenWorkspace_Refused`, `WidgetStatus_Frozen_ActiveTrue_PausedTrue`, `LoginWithKey_ResponseCarriesFreezeFields`, `KeySession_EventsSummary_Returns423`, `DangerLimiter_PartitionsByIdentity`, `StateService_SoftDeletedWorkspace_NotFrozen`, `ConvertedDemoInVerifyWindow_CanPause`, `Confirm_AfterLeave_LinkInvalid`, `WorkspaceResponse_TenantB_SeesNoStateOfA`, `PauseInstead_RacingConfirm_NoConstraintViolation`, `Job_CancelledBetweenQueryAndDelete_Skipped_NoFileDelete`) is now written, spread across `Db18WorkspaceLifecycleTests.cs`, `UserGovernanceTests.cs`, `InviteServiceTests.cs`, `WidgetActivationTests.cs`, `ApiKeyAuthTests.cs`, `WorkspaceFrozenFilterTests.cs`, `AuthRateLimitingTests.cs`, `WorkspaceServiceTests.cs` — all pass; no product bug found. | `bbe68b1`, `d162874` |
| G7 | HIGH — `SweepOnceAsync` resets the failure counter on every skip, hiding real faults | **Fixed (root cause + mechanism)** — the underlying reason skips happened at all (reminders never stamped, G1/O2) is fixed, so the "reminder not yet sent" skip is now transient/expected, not a masked permanent failure; additionally the skip path now catches only the new dedicated `DeletionPreconditionChangedException` (G/O #`DeletionPreconditionChangedException`, see O4) rather than every `InvalidOperationException`, so an unrelated bug of that base type now falls through to the real-failure counter instead of being silently treated as a benign race. | `eecb6cc` |
| G8 | MEDIUM — Operator Pause/Resume lack transaction/lock/concurrency handling | **Fixed** — both wrapped in `ExecuteInTransactionAsync` with the `FOR UPDATE` marker, tracked load via `IgnoreQueryFilters()`, and `DbUpdateConcurrencyException` → `Conflict(StateChanged)`. `OperatorResumeAsync` also now reschedules a past-due/<24h schedule to `now+24h` (clears the reminder flag, audits `deletion_rescheduled`) — folds in O8 below. | `eecb6cc` |
| G9 | MEDIUM — `StillDueAsync` omits `DeletedAt == null` for `owner_requested` | **Fixed** — added for consistency with every other lifecycle predicate (defense in depth; nothing currently soft-deletes a workspace ahead of hard-delete). | `eecb6cc` |
| G10 | MEDIUM — `Cancel`/`OperatorCancel` tracked loads inconsistently omit `IgnoreQueryFilters()` | **Fixed** — added to both, matching every other write in the class. | `eecb6cc` |
| G11 | MEDIUM — CLI exits 1 instead of 2 on an unhandled 423 | **Fixed** — `main()`'s top-level catch in `cli.ts` now maps `ApiError.code === 423` to exit 2. Re-checked `mark.ts`'s `--mark all` ordering the finding also named: `fetchQueue` already runs BEFORE `commitAll` in the current code (not the bug described) — no change needed there. | `26c1360`, `7f1d8e7` |
| G12 | LOW — `WorkspaceFrozenFilter` attribute reflection (`inherit: false`, not `EndpointMetadata`) | **Rejected** — every controller in this codebase declares `[AllowWhenWorkspacePaused]` directly (no inheritance chain reaches it), so the two approaches are behaviourally identical today; tried switching to `EndpointMetadata`, but `WorkspaceFrozenFilterTests.cs`'s hand-built `ActionExecutingContext` does not populate `EndpointMetadata` the way real ASP.NET Core routing does, so the switch silently broke 4 tests that only a real host would have caught. Reverted; left a comment recording the trade-off. | — (considered in `eecb6cc`'s working tree, reverted before commit) |
| G13 | LOW — `TenantsController` operator pause/resume missing `IsConflict` | **Fixed** — both now check `IsConflict` + `[ProducesResponseType(..., 409)]` (consistent with the new locked/re-checked operator methods, G8). | `eecb6cc` |
| G14 | LOW — missing `ck_workspaces_deletion_reminder_consistent` constraint | **Rejected** — already enforced: `ck_workspaces_deletion_schedule_consistent`'s third clause (`WorkspaceMapping.cs`) is exactly `(deletion_reminder_sent_at IS NULL OR deletion_scheduled_for IS NOT NULL)`. A separate constraint would be redundant, not missing behaviour. No migration edit needed (verified the existing migration already has exactly 8 AddColumn / 1 CreateIndex / 3 AddCheckConstraint, matching §3.2/§7.1). | — |
| G15 | NIT — DB-RULES R16 amendment + R19 missing | **Fixed** — both appended verbatim per the execution doc §5 task 19. | `893fe26` |
| G16 | NIT — redundant `TryRequireOwner` in `WorkspaceLifecycleGuard.CanManageAsync` | **Fixed** — simplified to a plain `TenantId != workspace.Id` comparison per the suggested fix. | `eecb6cc` |

### Opus (code-reviewer)

| # | Row | Verdict | Commit |
|---|---|---|---|
| O1 | BLOCKER — confirm/pause-instead tracked reload uses the tenant-filtered `Workspaces` | **Fixed** — same as G1. | `eecb6cc`, `bbe68b1` |
| O2 | BLOCKER — hosted reminder never stamps → 7-day-grace deletions never happen | **Fixed** — same as G1; also confirmed `ExecuteDueDeletionAsync`'s own due-query already had `IgnoreQueryFilters()` and needed no change. | `eecb6cc`, `bbe68b1` |
| O3 | BLOCKER — `SpendLinkAsync` same filter bug | **Fixed** — same as G1, plus the state re-check under lock (O5). | `eecb6cc`, `bbe68b1` |
| O4 | HIGH (tests) — every anonymous/job test uses a tenant-scoped/super-admin caller | **Fixed** — 6 new `NoTenantCurrentUser`/no-superadmin facts (§ G1); `Reminder_*` tests added (§ O11). | `bbe68b1` |
| O5 | MEDIUM — `SpendLinkAsync` no state re-check under lock | **Fixed** — now passes the validated `requestedAt`/`requestedBy` through, spends only if `DeletionScheduledFor == null` and both match the row it re-reads under lock, and catches `DbUpdateException` (not just the concurrency subtype) around the whole locked block. | `eecb6cc` |
| O6 | MEDIUM — link spend rides the shared lockout threshold | **Fixed** — same as G3 (per-link `IMemoryCache` counter, partitioned by `sub`+IP is N/A here — keyed by workspace+requestedAt instead, matching the suggested `delws:{ws:N}:{requestedAtMs}` shape exactly). | `eecb6cc`, `bbe68b1` |
| O7 | MEDIUM — job treats every `InvalidOperationException` as a race skip | **Fixed** — new `DeletionPreconditionChangedException : InvalidOperationException`, thrown only by the locked re-check's genuine precondition race; `WorkspaceDeletionService`'s catch narrowed to that type, falling through to the real-failure/Critical path for anything else. | `eecb6cc` |
| O8 | LOW — re-validation inside lock reads stale identity/membership | **Fixed** — `_unitOfWork.ClearChangeTracker()` added immediately after the `FOR UPDATE` marker in both `ConfirmDeletionAsync` and `PauseInsteadAsync`, before the re-validation's tracking queries (`FindIdentityByPublicIdAsync`/`GetMembershipAsync`). | `eecb6cc` |
| O9 | LOW — Confirm checks `PausedByOperator` only pre-lock | **Fixed** — added the same check on the freshly-loaded `tracked` row inside the lock. | `eecb6cc` |
| O10 | LOW — admin Pause/Resume write without lock/re-check | **Fixed** — both now run inside `ExecuteInTransactionAsync` with the `FOR UPDATE` marker and re-check every precondition (`PausedAt`, `PausedByOperator`, `DeletionScheduledFor`) on the freshly-loaded row before writing. | `eecb6cc` |
| O11 | LOW — operator-paused workspace still gets E3; resume doesn't reschedule a past-due schedule | **Fixed** — `SendDueRemindersAsync`'s candidate query and locked re-check both add `&& !w.PausedByOperator`; `OperatorResumeAsync` reschedules a `DeletionScheduledFor <= now+24h` row to `now+24h`, clears the reminder flag, and audits `workspace.deletion_rescheduled` (System actor). New tests: `Reminder_RacingCancel_NoConstraintViolation`, `Reminder_Missed_ReschedulesPlus24h_Audited`, `Reminder_GraceUnder2Days_NotSent`. | `eecb6cc`, `bbe68b1` |
| O12 | LOW — `BCrypt.Verify` throws on an empty/non-bcrypt hash | **Fixed** — `BcryptPasswordHasher.Verify` now guards `IsNullOrEmpty` and catches any exception from `BCrypt.Net.BCrypt.Verify`, returning `false` instead of a 500. | `eecb6cc` |
| O13 | LOW — grant guard refuses no-op fields | **Fixed** — `UserService.UpdateAsync`'s frozen-grant guard now compares against current state (`!IsNullOrEmpty(Password) \|\| (IsActive==true && !membership.IsActive) \|\| (role changes AND (GrantsAdmin OR a QuickAccess→full-member upgrade))`), folding in O14 too. | `eecb6cc` |
| O14 | LOW — grant guard misses QuickAccess → full-member upgrade | **Fixed** — folded into O13's condition (`wasQuickAccess && !role.QuickAccess`). | `eecb6cc` |
| O15 | LOW — `POST users/{id}/reject` removes access but is refused while frozen | **Fixed** — added `[AllowWhenWorkspacePaused]`; `WorkspaceFreezeCoverageTests`'s pinned list updated to include it. | `eecb6cc`, `bbe68b1` |
| O16 | LOW — widget add-comment shortcut still starts picking while paused | **Fixed** — `startPicking()` now no-ops while `_paused`. New tests: no-op while paused, still works while not. | `cdbe41b`, `f1f896e` |
| O17 | LOW — widget first-upload 423 shows two toasts | **Fixed** — `createComment`'s screenshot-upload branch now only toasts its own "upload failed" message when `!this._paused` (uploadToServer's own 423 branch already toasted once). | `cdbe41b` (not independently unit-tested — `CreateCommentData` scaffolding cost was judged not worth it for a one-line guard already covered by code inspection + the existing `uploadToServer` 423 tests) |
| O18 | LOW — CLI 423 mid-run → generic exit; `--mark` may have committed locally | **Fixed the exit-code half** — same as G11. **Re-checked the `--mark all` ordering half**: `mark.ts`'s `all` branch already does `fetchQueue` BEFORE `commitAll` in the current code — the described "commits before it knows what's pending" bug does not exist in this branch (possibly already fixed at some point before this review, or the review's line numbers referred to a different revision); no change made there. | `26c1360`, `7f1d8e7` |
| O19 | NIT — "held by operator pause" log unreachable | **Rejected** — verified it IS reachable: `WorkspaceDeletionService`'s `dueIds` query excludes `PausedByOperator` rows, but a workspace can become operator-paused in the gap between that query and the per-item `ExecuteDueDeletionAsync(id)` call for an id it already selected — exactly the race `Job_OperatorPausedDuringGrace_NotDeleted` (pre-existing test, still green) exercises by forcing that state directly. Dropping the filter from `dueIds` as suggested would be worse: every already-known-held row would re-attempt (and re-fail) the guarded pre-check on every 15-minute sweep for no benefit. | — |
| O20 | NIT — `pointer deployed` says "paused" when scheduled for deletion | **Fixed** — both call sites now print `err.message` (the server's own, state-specific message) instead of a hard-coded string. | `26c1360`, `7f1d8e7` |
| O21 | NIT — `NormalizeName` uses literal invisible characters | **Fixed** — switched to `‎`/`‏`/`؜`/`​` escapes. | `eecb6cc` |
| O22 | NIT — paused notice shown as an 'error' toast | **Fixed** — `_handlePaused()` now calls `this.toast(t('paused.notice'))` (default/info kind). | `cdbe41b`, `f1f896e` |
| O23 | HIGH (tests) — Sqlite race tests missing | **Fixed in `d162874`** — the remaining two, `PauseInstead_RacingConfirm_NoConstraintViolation` and `Job_CancelledBetweenQueryAndDelete_Skipped_NoFileDelete`, are now written using the same InMemory `RacingUnitOfWork` technique as `Confirm_Concurrent_OnlyOneSchedules`/`Cancel_WhileJobHoldsLock_Conflict` (roles reversed in each case); both pass. | `bbe68b1`, `d162874` |
| O24 | HIGH (tests) — preview parity / HardDelete owner_requested refusal tests missing | **Fixed in `d162874`** — `HardDelete_OwnerRequested_NotScheduled_Refused_NoAudit_NoFiles` and `HardDelete_OwnerRequested_OperatorPaused_Refused` added (drive `TenantService.HardDeleteAsync` directly with a `RecordingFileStorage`, asserting no audit row and no file delete on refusal); `Job_DueDeletion_HardDeletes_AuditsSystemActor_AndDeletesFiles` added (drives the real `ExecuteDueDeletionAsync` → `HardDeleteAsync("owner_requested")` path, asserting the file delete AND the System-actor audit row — the pre-existing `ExecuteDueDeletionAsync_NoTenantCurrentUser_HardDeletes` only asserted the row was gone). All pass; no product bug found. | `bbe68b1`, `d162874` |
| O25 | MEDIUM (tests) — long list of freeze/coverage tests missing | **Fixed in `d162874`** — every test this row named is now written: `Freeze_AllowsRevocations_RefusesGrants` (`UserGovernanceTests.cs`), `InviteAccept_FrozenWorkspace_Refused` (`InviteServiceTests.cs`), `Register_FrozenWorkspace_Refused` + `Register_ReapplyAfterRejection_FrozenWorkspace_Refused` (`Db18WorkspaceLifecycleTests.cs`, new `AuthService` fixture), `WidgetStatus_Frozen_ActiveTrue_PausedTrue` (`WidgetActivationTests.cs`), `LoginWithKey_ResponseCarriesFreezeFields` (`ApiKeyAuthTests.cs`), `KeySession_EventsSummary_Returns423` (`WorkspaceFrozenFilterTests.cs`), `DangerLimiter_PartitionsByIdentity` (`AuthRateLimitingTests.cs`), `StateService_SoftDeletedWorkspace_NotFrozen` + `ConvertedDemoInVerifyWindow_CanPause` + `Confirm_AfterLeave_LinkInvalid` (`Db18WorkspaceLifecycleTests.cs`), `WorkspaceResponse_TenantB_SeesNoStateOfA` (`WorkspaceServiceTests.cs`). All pass against the existing implementation; none uncovered a bug. | `d162874` |
