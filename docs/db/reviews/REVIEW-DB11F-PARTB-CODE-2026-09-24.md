# DB-11f Part B code review — 2026-09-24 (branch feat/db-11f-part-b)

## Gemini 3.8 Flash (agy)

| severity | finding | evidence file:line | suggested fix |
| :--- | :--- | :--- | :--- |
| BLOCKER | Part B deployment timing violation: Part A was deployed at `2026-09-24 05:05 UTC`, so the mandatory 24-hour production soak window has not elapsed. DB-11f (§9 step 5 and header) strictly forbids merging or applying Part B migrations before `2026-09-25 05:05 UTC` and prior to completing the post-window P1 and P11 invariant/error verification queries. | `DEPLOY.md:148`<br>`docs/db/execution/DB-11f-drop-legacy-users-tenancy-columns.md:19`<br>`docs/db/execution/DB-11f-drop-legacy-users-tenancy-columns.md:609-613`<br>`docs/db/execution/DB-11f-drop-legacy-users-tenancy-columns.md:733` | Hold merge and deployment until after `2026-09-25 05:05 UTC`, verify 0 invariant violation logs in production, and run P1/P11 SQL checks before executing Part B. |
| MEDIUM | Test 7 (`HardDelete_RehomedIdentityWithTenantRole_Succeeds_UnderRealForeignKeys`) was deleted outright instead of being adapted to Part B. While doc §6 item 16 only authorized deleting legacy pointer tests 6, 8, and 8b, Test 7 was the only test in the suite enforcing real SQLite foreign keys (`PRAGMA foreign_keys = ON`) when deleting a workspace owning a custom role while a surviving multi-workspace identity retained memberships elsewhere (`WorkspaceMembershipTests:544` only exercises multi-workspace survival under global roles). | `Tests/Db11fMembershipRulesTests.cs:1106-1113`<br>`docs/db/execution/DB-11f-drop-legacy-users-tenancy-columns.md:688-692` | Re-instate Test 7 adapted to Part B: seed identity with `RoleId = null`, workspace W owning custom role R, membership in W on R and membership in workspace X on a global role; verify `HardDeleteAsync(W)` succeeds under SQLite foreign keys, R is deleted, and surviving identity retains `RoleId == null`. |
| LOW | Test 18 implementation is incomplete against the DB-11f §6 item 18 specification. While parts (1) and (2) (`NewIdentity` and demo provisioning leave `RoleId == null`) are implemented, part (3) — verifying that executing `AdminSeeder` against an empty database leaves the seeded super-admin identity with `RoleId == SuperAdminRoleId` — was omitted from Test 18. | `Tests/Db11fMembershipRulesTests.cs:2227-2275`<br>`docs/db/execution/DB-11f-drop-legacy-users-tenancy-columns.md:811-812` | Add the empty-database `AdminSeeder` execution and assertion block to Test 18 as prescribed by §6 item 18. |
| NIT | `SoftDeleteUniqueIndexTests` test harness executes raw SQL to recreate `ux_users_email_live`. This change is justified: `ux_users_email_owner_live` (EF-modeled) was dropped in B-1, while `ux_users_email_live` is unmodeled in EF metadata (created via raw SQL in migration `20260922205015` due to Postgres `lower()`). EF's `EnsureCreated()` in SQLite skips unmodeled DDL, requiring manual index creation in test setup to validate soft-delete uniqueness. | `Tests/SoftDeleteUniqueIndexTests.cs:56-58` | Keep raw DDL bootstrap in test harness as-is. |
| NIT | Test 5 local helper `Make(string email, Guid? ownerId, int roleId, ...)` retains unused parameter `Guid? ownerId`. The argument is never assigned to the `User` entity since `User.OwnerId` was removed. | `Tests/Db11fMembershipRulesTests.cs:1023-1040` | Remove the unused `ownerId` parameter from `Make` helper method signature and invocations in Test 5. |

Orchestrator: the BLOCKER is the release timing (Part B not before 2026-09-25 05:05 UTC) — already enforced by the release plan, not a code defect.

## Opus (code-reviewer)

MEDIUM: InviteServiceTests.cs:1831/1885 quick-access refusal tests assert users.role_id (vacuous); Test 18 builds DemoService with NoopAuditWriter so a re-added demoUser.Role = role would never be saved (and misses the AdminSeeder empty-DB leg); test 7 deleted beyond doc §6 item 16 — rewrite for Part B (real-FK Sqlite, survivor with W-owned role membership). LOW: nothing enforces users.role_id ∈ {global super role}; B-2 Down silently no-ops without a least-privilege role then B-1 Down fails half-rolled-back (add RAISE guard); §7 B6/§8 rollback 'psql -1' not atomic (EF script has per-migration transactions); SoftDeleteUniqueIndexTests harness change justified (share the DDL constant). NIT: stale comments (TenantService.cs:212-213, AuthService.cs:1451-1452/1218-1220, AppDbContext.cs:107); SCHEMA/DB-REVIEW stamping at deploy.
