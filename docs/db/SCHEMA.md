# Schema map (as of 2026-09-23, `main` @ `790b9b8`; 72 migrations, newest `20260923045038_AddImpersonationSessions`)

> Production note (2026-09-23): 72 migrations now in production (newest `20260923045038_AddImpersonationSessions`),
> after today's DB-11a, DB-11b, DB-12 (parts 1 + 2), R5-62, DB-11c, DB-11d, and DB-13 deployments (see
> `DB-REVIEW-2026-09-22.md` §7).
>
> **DB-11a deployed to production 2026-09-23 00:08 UTC** (`POINTER_APPLY_CONTRACT=1
> POINTER_CONTRACT_LABEL=pre-db11a`, migrations 65→68); census 0 duplicate e-mails → 0 merges, 0
> aliases; 5 live memberships (6e4b3406:1, 98699076:4); `ux_users_email_live` and
> `ux_api_keys_active_per_membership` confirmed present.
>
> **DB-12 parts 1 & 2 and DB-11b deployed to production 2026-09-23**: part 1 (`pre-db12` contract
> deploy, commit `30d1b46`, 70 migrations, newest `20260923003835_AddAuditEventsAppendOnlyTrigger`);
> part 2 deployed 03:42 UTC (commit `fda2717`; 103 attributed actions, 20 services; Security log page live).
>
> **DB-11c deployed to production ~04:55 UTC 2026-09-23** (merged `af4f98b` + fixes `21cb7ee`;
> 71 migrations in production, newest `20260923040830_AddUsersErasedAt`). `users.erased_at` is live.
>
> **DB-11d deployed to production ~05:50 UTC 2026-09-23** (merged `f9d21de` + fixes `c0fbad8`; no
> migration). `POST /api/me/change-email` and anonymous `POST /api/auth/confirm-email-change` live.
>
> **DB-13 deployed to production ~06:15 UTC 2026-09-23** (merged `441a462` + `5f1fbc8` + fixes
> `790b9b8`; 72 migrations, newest `20260923045038_AddImpersonationSessions`). Founder decision F2 is
> live: super admin sees metadata only; content only under an audited, time-boxed (≤ 60 min), read-only
> impersonation session; `impersonation_sessions` is live.
>
> **DB-14 implemented 2026-09-23, not yet deployed** (`users.email_verified_at`, migrations
> `20260923062123_AddUsersEmailVerifiedAt` + `20260923062150_BackfillUsersEmailVerifiedAt`,
> `[ContractMigration("DB-14")]`): ships via `POINTER_APPLY_CONTRACT=1
> POINTER_CONTRACT_LABEL=pre-db14` (or batched `pre-db12-14`, R7.1) — production stays at 72
> migrations until this deploys, adding two more (newest `20260923062150_BackfillUsersEmailVerifiedAt`)
> when it does — see `docs/db/execution/DB-14-email-verification-and-password-policy.md` §9.
>
> Still planned: DB-14 `users.email_verified_at` (implemented, not yet deployed — see above), DB-15
> `usage_daily` + three server-emitted `usage_events.type` values and a second partial unique index (30
> tables when all ship). Their rows below are marked *(planned)*. DB-12's `audit_events`, DB-11c's
> `users.erased_at`, and DB-13's `impersonation_sessions` describe the live production schema.

Plain-language map of the 25 tables, who owns each row, which query filter applies, and the
invariants that hold. Regenerated by the db-architect on each review rather than patched. Source of
truth is `Infrastructure/Migrations/AppDbContextModelSnapshot.cs` + `Infrastructure/Mappings/*.cs`.

**Conventions that hold everywhere:** `snake_case` physical names; `int identity` PK `id` (except
`usage_events`, still int but not a `BaseEntity`); audit columns `created_at/created_by/updated_at/
updated_by/deleted_at/deleted_by` stamped in `AppDbContext.SaveChangesAsync`
(`Infrastructure/AppDbContext.cs:168-194`); soft delete = `deleted_at IS NOT NULL`, enforced by
explicit predicates, not a global filter; all timestamps `timestamptz`; enums stored as `int`.

**Tenancy today:** a workspace is the uuid in `owner_id`; it equals the founding Workspace Admin's
`users.public_id`; it is the JWT `tenant` claim (`Infrastructure/Auth/JwtTokenService.cs:33-34` →
`HttpCurrentUser.TenantId`, `Infrastructure/CurrentUser/HttpCurrentUser.cs:24-27`). Super admins
have `owner_id = NULL` and bypass every filter. No table has an FK on `owner_id` (fixed by DB-03).

Filter shapes (`AppDbContext.cs:76-147`):
- **strict-own** — `IsSuperAdmin || OwnerId == TenantId` (+ legacy null-bucket branch, disabled in prod by `Tenancy:StrictNullTenantIsolation=true`, `docker-compose.prod.yml:30`)
- **own+global** — strict-own **or** `OwnerId IS NULL` (seeded catalog rows every tenant sees)
- **none** — global catalog / exempt

| Table | Entity | `owner_id` | Filter | Real FKs (behaviour) | Unique indexes | Notes / invariants |
|---|---|---|---|---|---|---|
| `workspaces` *(DB-03, shipped)* | `Workspace` (not `BaseEntity`) | is the PK (`uuid`) | strict-own on `Id` (super admin sees all) | — | `ck_workspaces_name_not_blank` | One row per tenant; every `owner_id` on the other 22 tables now FKs to it (`fk_<table>_workspaces_owner_id`, Restrict, except `usage_events` SetNull). `name varchar(120)` is the workspace's **own** name (Q3) — backfilled as the placeholder `Workspace`, set by the admin via DB-03b; no code reads a workspace label from `users` after DB-03 |
| `users` | `User` | nullable (NULL = super admin, legacy elsewhere) | membership-based (`e.Memberships.Any(m => m.OwnerId == tenant)`, DB-11a) | `role_id → roles` (Restrict); `merged_into_user_id → users` (self FK, Restrict) | `public_id`; `ux_users_email_owner_live (email, owner_id) WHERE deleted_at IS NULL NULLS NOT DISTINCT` (DB-05, kept — dropped by DB-11e); `ux_users_email_live UNIQUE ON lower(email) WHERE deleted_at IS NULL` (DB-11a, raw SQL, not modelled) | **DB-11a:** one row per e-mail (**the identity**). `owner_id`/`role_id`/`approval_status` are legacy (written once at creation, never read after this doc; dropped by DB-11e). `merged_into_user_id` non-null ⇔ this row was merged by the same-e-mail merge and is soft-deleted. Carries demo fields (`is_demo`, `expires_at`, caps) — S-8. **DB-11c (2026-09-23):** `erased_at` present — non-null = tombstone (`erased+<id>@tombstone.invalid`, `Deleted user`, `deleted_at` set, `public_id` kept). **DB-14 (2026-09-23, not yet deployed):** `email_verified_at` — non-null = proved control of `email` (verification link, addressed invite, operator-created, or the DB-14 grandfathering backfill); null = unverified, gated from admin writes by `RequireVerifiedEmailFilter` |
| `workspace_memberships` *(DB-11a, shipped)* | `WorkspaceMembership` | NOT NULL (= the workspace) | strict-own | `user_id → users` (Restrict); `owner_id → workspaces` (Restrict); `role_id → roles` (Restrict); `invite_id → invites` (SetNull) | `(user_id, owner_id) WHERE left_at IS NULL AND deleted_at IS NULL`; `(owner_id, role_id)` | One row per (identity, workspace): `role_id`, `is_active`, `approval_status`, `security_stamp` (JWT `mstamp`), `joined_at`, `left_at`/`left_reason` (Removed=1, Left=2, AccountErased=3 — ended rows are kept). Invariant: every workspace keeps ≥1 live `Workspace Admin` membership (DB-11c guard, deployed 2026-09-23) |
| `user_aliases` *(DB-11a, shipped)* | `UserAlias` (not `BaseEntity`, uuid PK) | — (no filter; lookup by a uuid the caller already holds) | none | `user_id → users` (Cascade) | PK `alias_public_id` | Every `public_id` that existed before the same-e-mail merge → the identity it now belongs to. `UserNameResolver` falls back to it; content uuids were rewritten in place once (DB-11a §3.2 2.5) |
| `roles` | `Role` | nullable (NULL = global seeded) | own+global | — | `(name, owner_id)` **unfiltered** + raw `ix_roles_name_global` (DB-05) | Seeded: Admin, Workspace Admin, Workspace Admin Deputy, Developer, PM, Tester, Client (`AdminSeeder.cs:16-25`). Role **names** are load-bearing (45 refs) |
| `role_tenant_overrides` | `RoleTenantOverride` | NOT NULL | strict-own | none (`role_id` logical → DB-06) | `(role_id, owner_id)` unfiltered (DB-05) | Per-tenant `is_active` override of a global role |
| `projects` | `Project` | NOT NULL | strict-own | — | `(key, owner_id) WHERE deleted_at IS NULL`; raw `ix_projects_key_global` (empty, DB-05 drops) | Per-env activation flags; legacy `app_url`; JSON-in-varchar `tech_stack`, `ai_tools_used`, `environment_selector_role_ids` |
| `project_app_urls` | `ProjectAppUrl` | NOT NULL | strict-own | `project_id → projects` (Cascade, inverse `Project.ProjectAppUrls` named since DB-04); `app_environment_id → app_environments` (Cascade — latent footgun only if a hard-delete path for global environments ever appears, DB-06 §10) | `(project_id, app_environment_id) WHERE deleted_at IS NULL` | One URL per project per environment. Shadow `ProjectId1` **dropped** by DB-04 (`20260922080137`, deployed 2026-09-22) |
| `app_environments` | `AppEnvironment` | nullable (NULL = global seeded) | own+global | — | `(name, owner_id)` unfiltered (DB-05) | Seeded `local, prod, staging, testing`; `default` retired (`is_retired`) |
| `project_builds` | `ProjectBuild` | nullable | strict-own (no null branch) | `project_id → projects` (Cascade) | `(project_id, sha) WHERE deleted_at IS NULL` | Deploy awareness: fact that a sha shipped |
| `comments` | `Comment` | NOT NULL | strict-own | `project_id → projects` (**Restrict**); `page_context_snapshot_id → page_context_snapshots` (SetNull) | — (perf: `(project_id, status)`, `(project_id, created_at) WHERE deleted_at IS NULL`, `(owner_id, created_at) WHERE …`) | `author_id` = `users.public_id`, no FK (R14). jsonb: `element` (owned), `picked_actions` (owned), `payload_flags`, `custom_fields`. `body ≤ 4000` |
| `replies` | `Reply` | nullable (never written NULL) | strict-own | `comment_id → comments` (Cascade) | — | jsonb `payload_flags`; `is_ai`, `ai_tool`, `ai_model` |
| `page_context_snapshots` | `PageContextSnapshot` | nullable (never NULL) | strict-own | `project_id → projects` (Cascade) | — (lookup `(project_id, route, environment, session_id)`) | jsonb `console_entries`, `network_entries`; no retention (DB-08) |
| `notifications` | `Notification` | nullable | strict-own | `comment_id → comments` (Cascade); `project_id → projects` (**Restrict** — blocks tenant delete, DB-06 → Cascade); `suggestion_id → predefined_action_suggestions` (Cascade) | — (`(user_id, read_at)`) | `user_id`/`actor_id` = `users.public_id`; jsonb `payload`; no retention (DB-08) |
| `predefined_actions` | `PredefinedAction` | NOT NULL | own+global (null never written since `20260827130246`) | none (`project_id` logical → DB-06) | — | Text ≤256, prompt `text` (never sent to browser) |
| `predefined_action_suggestions` | `PredefinedActionSuggestion` | nullable (never NULL) | strict-own | none (`project_id` logical → DB-06) | — | Status enum int; `admin_feedback text` |
| `ai_rules` | `AiRule` | NOT NULL | strict-own | none (`project_id` logical → DB-06) | — | Scope by `project_id`/`user_id` nullability |
| `status_presentations` | `StatusPresentation` | nullable (NULL = global) | own+global | — | `(status_value, owner_id)` unfiltered + raw `_global` (DB-05) | Per-tenant label/colour for `CommentStatus` ints |
| `workspace_settings` | `WorkspaceSetting` | nullable (never NULL) | strict-own | — | `owner_id WHERE deleted_at IS NULL, NULLS NOT DISTINCT` | jsonb `comment_field_definitions`; one row per workspace; future §32 `webhook_url` lives here |
| `invites` | `Invite` | nullable (NULL = "new workspace" invite) | strict-own | none (`role_id`, `project_id`, `plan_id` logical → DB-06) | `code` | `uses`/`max_uses`, `revoked_at`, `expires_at` — expired rows never pruned (DB-08) |
| `quick_access_links` | `QuickAccessLink` | nullable (never NULL) | strict-own (no null branch) | none (`user_id(uuid)`, `project_id`, `invite_id` logical → DB-06) | `token_hash` | Magic-link credential; raw token never stored |
| `api_keys` | `ApiKey` | nullable (mirrors the membership) | strict-own | `user_id → users` (Cascade) — a `users.id` FK (structural child, R14) | `hash`; `ux_api_keys_active_per_membership (user_id, owner_id) WHERE revoked_at IS NULL AND deleted_at IS NULL NULLS NOT DISTINCT` (DB-11a, replaces `ux_api_keys_active_per_user`) | AES-GCM `encrypted` + SHA-256 `hash`. DB-11a: a key is per (identity, workspace); `owner_id` is the workspace it logs into |
| `device_logins` | `DeviceLogin` | nullable (stamped on approval) | **none** (exempt, anonymous endpoints) | none (`user_id` uuid logical) | `device_code_hash` | Swept inline > 1 day past expiry (`DeviceLoginService.cs:47-56`) |
| `subscriptions` | `Subscription` | NOT NULL | strict-own | `plan_id → plans` (Restrict) | `owner_id` unfiltered (DB-05) | Missing row ⇒ Free; payment-ready columns unused (Noop provider) |
| `plans` | `Plan` | — | none (global catalog) | — | `name`, `slug` **unfiltered** (DB-05 → `ux_plans_name_live`, `ux_plans_slug_live`) | jsonb `feature_bullets`, owned `entitlements`; seeded Free + hidden Legacy; `PlanService.DeleteAsync` soft-deletes (`PlanService.cs:132`) |
| `extension_sites` | `ExtensionSite` | NOT NULL (`Guid`, not `Guid?`) | strict-own | — | `(owner_id, origin)` unfiltered (DB-05) | Backs `MaxExtensionSites` |
| `usage_events` | `UsageEvent` (not `BaseEntity`) | nullable | strict-own | `owner_id → workspaces` (**SetNull**, DB-03); `project_id → projects` (SetNull, DB-06) | `ux_usage_events_first_per_project (project_id, type) WHERE type IN ('first_comment','first_apply')`; **planned (DB-15)** `ux_usage_events_widget_installed_per_project (project_id, type) WHERE type = 'widget_installed'` | Analytics; jsonb `meta`; DB-08 sweeps rows > 180 d **except** the one-shot facts. **Planned (DB-15):** server-only types `demo_started`, `workspace_converted`, `widget_installed` join `first_comment`/`first_apply` as never-swept funnel facts (`UsageEventTypes.OneShotFacts`); clients may still post only `installed, doctor_run, apply_started, apply_failed, widget_language`. Exempt from `HardDeleteOrder` (R8.8) |
| `usage_daily` *(planned, DB-15)* | `UsageDaily` (not `BaseEntity`, bigint PK) | nullable (`SetNull` FK; NULL = no/deleted workspace) | strict-own | `owner_id → workspaces` (SetNull) | `ux_usage_daily_day_owner_type (day, owner_id, type) NULLS NOT DISTINCT` | Daily rollup `(day, owner_id, type, count, computed_at)` written by `RetentionService` **before** the usage sweep; last 3 days recomputed each pass; kept forever; R8.8 exempt |
| `audit_events` *(DB-12 parts 1 & 2 deployed 2026-09-23)* | `AuditEvent` (not `BaseEntity`, bigint PK, `init`-only) | nullable (`SetNull` FK; NULL = operator-level or deleted workspace) | strict-own (super admin sees all — metadata) | `owner_id → workspaces` (SetNull) | — (`(owner_id, occurred_at DESC)`, `(actor_user_id, occurred_at DESC)`, `(target_type, target_id)`) | **Append-only**: triggers `trg_audit_events_append_only` (UPDATE/DELETE) + `trg_audit_events_no_truncate`, plus a `SaveChangesAsync` guard; the FK's SET NULL is the one permitted update. Actor = `public_id` + membership id + kind (User/SuperAdmin/System/Impersonation); dotted `action` from `AuditActions`; `before`/`after` jsonb with whitelisted keys only; `request_id`, keyed `ip_hash`, `user_agent`, logical `impersonation_session_id`. Never swept (R17). No raw e-mail ever (`PseudonymHasher`) |
| `impersonation_sessions` *(DB-13, deployed 2026-09-23)* | `ImpersonationSession` (not `BaseEntity`, bigint PK) | nullable (`SetNull` FK; the impersonated workspace) | strict-own (a workspace lists sessions that targeted it; super admin all) | `owner_id → workspaces` (SetNull) | — (`(owner_id, started_at DESC)`, `(operator_user_id) WHERE ended_at IS NULL`) | `operator_user_id` (uuid, no FK), `reason`, `started_at`, `expires_at` (≤ 60 min), `ended_at`, `end_reason` (Manual/Expired), `request_count`, `last_request_at`. The JWT `imp` claim; the validator proves liveness per request; the `tenant` claim of an impersonation token is what admits content rows (the six content filters lose their unconditional super-admin branch). R8.8 exempt |
| `app_settings` | `AppSetting` | — | none (endpoint authz) | — | `key` unfiltered (DB-05 → `ux_app_settings_key_live`) | Global key/value; carries brand strings (rebranding §11.3); no delete path today |

**Cross-table facts worth remembering**

- Deleting a **project** hard: cascades `project_app_urls`, `project_builds`, `page_context_snapshots`; **blocked** by any `comments` or `notifications` (Restrict). Orphans (no FK): `predefined_actions`, `predefined_action_suggestions`, `ai_rules`, `quick_access_links`, `invites.project_id`, `usage_events.project_id`.
- Deleting a **user** hard: cascades `api_keys` and `user_aliases`; `workspace_memberships` is Restrict (must be removed first — `TenantService.HardDeleteAsync` does, before `users`); every uuid reference (`author_id`, `created_by`, …) dangles by design. Hard delete of a `users` row happens only inside a workspace hard delete and only when the identity has no membership elsewhere (DB-11a §3.5). **DB-11c (shipped 2026-09-23):** the normal path is *erase* = tombstone the row (keeps `public_id`, so nothing dangles; `users.erased_at` set).
- **Identity model (DB-11a, DB-11b, DB-11c shipped 2026-09-23):** a person = one `users` row per e-mail; workspace presence = `workspace_memberships`; a session = (identity, workspace) with JWT `sub` + `tenant` + `stamp` + `mstamp` (DB-RULES R16). `workspaces.id` no longer equals any `public_id` from DB-11a on (every mint point uses `Guid.NewGuid()`). DB-11c (shipped): workspace admins can remove/disable members without deleting identities; identities erased by the person themselves or a super admin; S-13 sole-admin guard prevents a workspace from losing its last admin without a transfer.
- Deleting a **workspace**: `TenantService.HardDeleteAsync` (`TenantService.cs:356-441`) handles 6 of 23 owner-carrying tables; see DB-03.
- Filtered indexes and value converters are **not** exercised by the InMemory test provider; Sqlite honours the partial index filter strings (column names are snake_case in both) but cannot run the Npgsql migrations (`EnsureCreated` only). Postgres behaviour is proven only by the R11 rehearsal.
