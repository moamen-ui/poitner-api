# R2-00-tests — Fresh-app init E2E + white-label CI job

Harness: [`00-HARNESS.md`](00-HARNESS.md). Execution doc: [`../execution/R2-00-e2e-fresh-app-whitelabel.md`](../execution/R2-00-e2e-fresh-app-whitelabel.md).

## Covers

AC-1 (`--fresh` green for all stacks) → R2-00-01…04 · AC-2 (vite+static init→first-comment ≤ 300 s, seconds printed, ≤ 1 retry) → R2-00-01/02 step 8 · AC-3 (angular **and `next`** assert the exact hand-off message + no `index.html`/`app/` edit) → R2-00-03/04 · AC-4 (whitelabel: human CLI output contains `Acme Review`, zero leak-regex matches; widget innerText, modal title, `title`/`aria-label` pass) → R2-00-05/06/08 · AC-5 (Task 0 leak fix — `grep '"Open Pointer feedback"' templates.ts` empty) → R2-00-08 step 4 · AC-6 (CI matrix + whitelabel job green on dispatch) → CI wiring, not a scenario row · AC-7 (branding restored, `GET /api/branding` → `Pointer`) → R2-00-07. The scenarios R2-00's Tests section merely *hosts* from other docs are cross-referenced under "Not covered here".

## Preconditions

- Seed complete (`state/credentials.json`, `state/keys.json`); personas used: DEV (`USERS.developer`), SA (`SUPER_ADMIN`). Role shorthand per testing-review §3.
- Prerequisite docs merged: R1-02 (`init`), R1-04 (`doctor`), R1-07 (workflow). CLI built: `cd cli && npm run build`; `CLI_ENTRY=node <repo>/cli/dist/cli.js`.
- `e2e/scripts/serve-dir.mjs` exists (R2-00 task 3b) and serves any dir on a port. **Decision:** the branding helpers live at `e2e/scripts/set-branding.mjs` + `e2e/scripts/reset-branding.mjs` (harness §4 layout is binding, superseding the execution doc's `e2e/whitelabel/` paths); both log in as `SUPER_ADMIN` and `PUT /api/admin/branding` (super-admin-gated, `Admin/BrandingController.cs:19`) using `BrandingWriteDto` field names `productName`, `tagline`, `urls.app`.
- Branding defaults for restore (`BrandingService.cs:9-17`): `productName "Pointer"`, `tagline "Point at the UI. Ship it with AI."`, `urls.app https://app.pointer.moamen.work`.
- **Widget reveal (binding, harness §9):** a fresh browser context starts collapsed — `element.ts:161-163` sets `_collapsed` unless `sessionStorage.pointer_visible === '1'`, and `renderChrome()` then emits **only** `TPL.launcher` and returns (`element.ts:649-655`). So `#pf-toggle` / `#pf-add` / `#pf-user` / `#pf-env` do not exist until the toolbar is revealed. Every widget step below either uses `preAuthWidget(page, token, user)` (which already sets `pointer_visible='1'`, `widget.spec.ts:41-51`) **or** clicks `widget.locator('#pf-launcher')` first.
- **Shadow-root text:** `widget.locator(':root')` matches nothing inside a shadow root. Use `page.locator('pointer-feedback').innerText()` for whole-widget text sweeps.
- **`preAuthWidget` is a 3-arg, non-exported local** (`e2e/widget/widget.spec.ts:41`). Task (R2-00 spec files): extract it to `e2e/widget/lib/pre-auth.ts` and import the 3-arg form everywhere; every doc below assumes that extraction.
- Fresh projects are created with `enforceAllowedOrigins` default **false**, so the widget renders on any origin; each spec still asserts the gate: `GET /api/public/projects/{key}/widget-status?origin=http%3A%2F%2Flocalhost%3A4174` → `data.active === true`.
- Generators pinned to a minor and run with npm cache present: `npm create vite@6.0 -- --template vanilla-ts`, `npx @angular/cli@20.0 new fresh-ng --defaults --skip-git --skip-install`, `npx create-next-app@15.0 --ts --app --no-eslint --use-npm --yes`.
- Ports: fresh preview **4174** (`--strictPort`); registry in `lib/constants.mjs`. Generated apps live under `e2e/state/fresh/<stack>/` (gitignored). **Both R2-00-01 (`vite preview`) and R2-00-02 (`serve-dir.mjs`) bind 4174 under `--strictPort`, so the fresh-app stacks run strictly sequentially** — one serial phase in `run-e2e.sh` (`workers: 1` for `fresh.spec.ts`), never a parallel matrix on one runner.
- `run-e2e.sh --fresh` / `--whitelabel` flags wrap the phases; **the whitelabel phase always runs `node e2e/scripts/reset-branding.mjs` in a `finally`** (harness §8).

## Scenarios

| id | intent | tier | layer | role | steps | expected | evidence |
|---|---|---|---|---|---|---|---|
| R2-00-01 | `fresh-app: vite` (= `init-vite-no-ai`) | **nightly** (retiered: `npm create` + install + build + a 300 s budget + one retry cannot fit the 15-min PR ceiling alongside R2-00-02 — harness §8; `static` is the PR-tier funnel proof) | cli + widget | DEV | 1. `node e2e/fresh-app/run.mjs --stack vite --api http://localhost:8090` — wipes `e2e/state/fresh/vite`, scaffolds `npm create vite@6.0 -- --template vanilla-ts`. 2. DEV key from `state/keys.json.developer`. 3. `spawnCli({ cwd: app, args: ['init','--server','http://localhost:8090','--key',DEV_KEY,'--create','Fresh vite <runId>','--environment','local','--tool','other','--yes','--json'] })`. 4. `spawnCli({ cwd: app, args: ['doctor','--json'] })`. 5. `npm install && npm run build`; `npx vite preview --port 4174 --strictPort` (run.mjs spawns, kills in `finally`). 6. Playwright `fresh.spec.ts` env `FRESH_URL=http://localhost:4174 FRESH_KEY=<project.key>`: `page.goto(FRESH_URL)`; assert `GET /api/public/projects/{key}/widget-status?origin=<FRESH_URL origin>` → `active:true`; host `pointer-feedback` present with `shadowRoot`; **reveal the toolbar first** — `widget.locator('#pf-launcher').click()` (fresh context is collapsed, `element.ts:161-163,649-655`); then click `widget.locator('#pf-toggle')` → login modal `.pf-modal-overlay` visible; **register `const cfg = page.waitForResponse('**/capture-config')` BEFORE submitting** (the request only fires from the post-login callback, `element.ts:673`); fill `#pf-email`/`#pf-password` with DEV creds → `#pf-login-submit`; `await cfg`; modal closes; `widget.locator('#pf-add').click()`; `page.locator('h1').first().click({ force: true })`; fill `#pf-comment-text` = `E2E fresh comment`; `#pf-popover-host` `#pf-submit` click → popover empty. 7. API: `get('/api/projects/{key}/comments?view=summary&pageSize=10', { token: devToken })` → exactly 1 item, `body === 'E2E fresh comment'`. 8. Budget: wall-clock from step 3 to step 7 ≤ 300 s; on breach one automatic whole-stack retry (timings of **every** attempt printed). | 3 → exit 0, `--json` summary: `ok===true`, `project.created===true`, `project.key` matches `/^fresh-vite-/`, `injected===true`, `routedToSkill===false`, `files` contains `.pointer/config.json`, `product==='Pointer'`; `.env` gains `VITE_POINTER_*`; `index.html` snippet inside `<!-- pointer-feedback:start/end -->` markers. 4 → exit 0 `ok:true`. 6 → comment created, sidebar count ≥ 1. 7 → 1 item as stated. 8 → pass or one-retry pass | report row with per-attempt seconds; CI artifact zip of `state/fresh/vite` on failure |
| R2-00-02 | `fresh-app: static` (= `init-static-no-ai`) | **PR** (paths `cli/**`) · nightly — the only PR-tier fresh-app stack (no `npm install`, no build; ~30 s) | cli + widget | DEV | 1. `run.mjs --stack static`: wipe `state/fresh/static`; `mkdir` + `cp e2e/fresh-app/templates/static/index.html`. 2–4. Same init/doctor calls as R2-00-01 steps 2–4 with `--create 'Fresh static <runId>'`. 5. `node e2e/scripts/serve-dir.mjs e2e/state/fresh/static 4174` (run.mjs spawns, kills in `finally`). 6–8. Same Playwright + API + budget steps as R2-00-01, **including the `#pf-launcher` reveal and the pre-registered `capture-config` waiter** (same selectors; static page's `<h1>` target). | init JSON: `project.key` matches `/^fresh-static-/`, `injected===true`, `routedToSkill===false`; `index.html` contains `<script src="http://localhost:8090/pointer.js" … defer>` + `<pointer-feedback …>` before `</body>` inside the markers; doctor `ok:true`; 1 comment; ≤ 300 s | report row with per-attempt seconds |
| R2-00-03 | `fresh-app: angular (skill-routed)` | nightly | cli | DEV | 1. `run.mjs --stack angular`: scaffold `fresh-ng` per the pinned command (skip-git, skip-install). 2. Snapshot `find . -type f | sort` + `sha256(src/index.html)`. 3. init (same flags, `--create 'Fresh angular <runId>'`). 4. doctor `--json`. 5. Diff the file snapshot; re-hash `src/index.html`. | init exit 0; JSON `injected===false`, `routedToSkill===true`; stdout contains the R1-02 §F hand-off message **verbatim** — assert `/^ℹ angular detected — automatic injection isn't supported for this stack yet\.$/m` (`{kind}` is the lowercase detection id from R1-02 §H; the em dash and apostrophe are literal) plus the `.agents/` skill path line (tool `other`); files diff contains **only** additions under `.pointer/**`, `.agents/**` and `.gitignore` (R1-02:260 permits the gitignore edit); `src/index.html` byte-identical; doctor `ok:true` | report row; stdout excerpt + file-diff list |
| R2-00-04 | `fresh-app: next (handoff)` | nightly | cli | DEV | 1. `run.mjs --stack next`: `npx create-next-app@15.0 --ts --app --no-eslint --use-npm --yes fresh-next` into `state/fresh/next` (pinned minor). 2. Snapshot files + `sha256(app/layout.tsx)` and `sha256(app/page.tsx)`. 3. init with `--create 'Fresh next <runId>' --tool other --yes --json`. 4. Diff snapshot; re-hash the two `app/` files. | init exit 0; `injected===false`, `routedToSkill===true`; stdout matches `/^ℹ next detected — automatic injection isn't supported for this stack yet\.$/m` (same verbatim rule as R2-00-03); diff = only `.pointer/**` + `.agents/**` + `.gitignore`; **no `app/` edit** — both hashes byte-identical | report row; hash pair pasted |
| R2-00-05 | `whitelabel: cli-output has no brand leak` | nightly | cli | SA, DEV | 1. SA login → `PUT /api/admin/branding` body `{ productName: 'Acme Review', tagline: 'Review anything', urls: { app: 'https://app.acme.test' } }` → 200; `GET /api/branding` → `data.productName === 'Acme Review'`. 2. `run.mjs --stack static --brand-check`: init **with** `--json --create 'Fresh brand <runId>'` (assert `product === 'Acme Review'`), then init again **without** `--json` and **with `--project <key returned by run 1>`** — never a second `--create` with the same name, which would 409 (R1-02 defines no re-`--create` semantics); capture **all** stdout+stderr of both init and `doctor` (human mode) to `e2e/state/whitelabel/cli-output.txt`. 3. Assert file contains `Acme Review`; count matches of leak regex `/(?<![-\w])Pointer(?![-\w])/g` (**`g` flag required** — without it `match()` caps at one) → 0. 4. `GET /skill.md` and `GET /pointer-init.md` raw bodies contain `http://localhost:8090` (server-origin placeholder rewritten, `API/Program.cs:192-223`). 5. `finally`: `node e2e/scripts/reset-branding.mjs`. | 1 → 200 + name set; JSON mode `product === 'Acme Review'`; captured human output: ≥ 1 `Acme Review`, 0 leak matches (`pointer-feedback`, `<pointer-feedback>`, `.pointer/` are allowed by the regex by construction); skills carry the origin; branding restored even on failure | report row; `cli-output.txt` artifact; leak-regex match count |
| R2-00-06 | `whitelabel: widget text has no brand leak` | nightly | widget | SA, DEV | Runs inside the R2-00-05 brand window (branding still `Acme Review`, static app served on 4174). **The browser context must be created *after* the branding PUT** — `loadBranding()` is fetched once per widget boot (`element.ts:263`), so a context opened earlier keeps `Pointer`: 1. `preAuthWidget(page, devToken, devUser)` (reveals the toolbar via `pointer_visible`); register `const cfg = page.waitForResponse('**/capture-config')` **before** `page.goto('http://localhost:4174/')`; `await cfg`. 2. Shadow text: `await page.locator('pointer-feedback').innerText()` (`:root` matches nothing inside a shadow root) does **not** match `/(?<![-\w])Pointer(?![-\w])/g`. 3. Login-modal title: a **second, non-pre-authed** context → `widget.locator('#pf-launcher').click()` (collapsed by default) → `widget.locator('#pf-toggle').click()` → modal `.pf-modal h2` (`templates.ts:17` uses `getBrandName()`) `innerText === 'Acme Review'`. 4. Toasts: trigger `hideOverlay` → `await expect(widget.locator('.pf-toast')).toHaveText(/Acme Review/, { timeout: 2000 })` — the toast self-removes after 2200 ms (`element.ts:1523-1529`), so assert immediately after the click. | 2 → 0 leak matches over the whole innerText; 3 → exact `Acme Review`; 4 → brand name in toast within 2 s, 0 leaks | report row; DOM dump of shadow root attached |
| R2-00-07 | branding restored after whitelabel run | nightly | api | SA | **Not a spec row — a `run-e2e.sh` phase step.** A Playwright/node spec cannot express "run even if the preceding specs failed"; instead `run-e2e.sh`'s whitelabel phase is `try { … specs … } finally { node e2e/scripts/reset-branding.mjs; node e2e/scripts/assert-branding-default.mjs; }` and the assert script records the row through `lib/report.mjs`. Checks: 1. `GET /api/branding` → `data.productName === 'Pointer'`, `data.tagline === 'Point at the UI. Ship it with AI.'`. 2. SA `GET /api/admin/branding` → `urls.app === 'https://app.pointer.moamen.work'`. | both GETs 200 with the exact `BrandingService.cs:9-17` defaults, regardless of the phase's spec results | report row (`R2-00-07 PASS` proves clean teardown for the next phase) |
| R2-00-08 | `whitelabel: widget title/aria-label have no brand leak` | nightly | widget | SA, DEV | Same brand window as R2-00-06, **context created after the branding PUT**: 1. Collapsed context (no `sessionStorage pointer_visible`): `page.goto('http://localhost:4174/')` → launcher `widget.locator('#pf-launcher')` visible; `title` and `aria-label` === `Open Acme Review feedback` (this is the Task 0 fix at `templates.ts:133`). 2. Expanded context (preAuth, which sets `pointer_visible`): evaluate **every** element inside the shadow root with a `title` or `aria-label` attribute (`widget.locator('[title], [aria-label]')` → `getAttribute` each); none of the values match `/(?<![-\w])Pointer(?![-\w])/g`. 3. `grep -n '"Open Pointer feedback"' web-component/src/templates.ts` (repo root, source check) → no output (AC-5). | launcher attrs exact; attribute sweep 0 leak matches; grep empty | report row; the swept attribute value list pasted |

## Spec files

- `e2e/fresh-app/run.mjs` — scaffold/init/doctor/build/serve driver; `--stack vite|static|angular|next`, `--brand-check`, `--api`, `--keep`; prints per-attempt seconds; one retry on budget failure (R2-00 tasks 1, 3b consumer).
- `e2e/fresh-app/fresh.spec.ts` — steps 6–7 of R2-00-01/02 and the widget halves of R2-00-05/06/08; imports `preAuthWidget(page, token, user)` from **`e2e/widget/lib/pre-auth.ts`**.
- `e2e/widget/lib/pre-auth.ts` — **new (task):** extract the 3-arg `preAuthWidget` currently private to `e2e/widget/widget.spec.ts:41-51` (localStorage `pointer_token`/`pointer_user` + sessionStorage `pointer_visible`) and re-import it there, so every widget doc can use one helper.
- `e2e/scripts/assert-branding-default.mjs` — **new (task):** the R2-00-07 teardown assertion, invoked from `run-e2e.sh`'s whitelabel `finally` after `reset-branding.mjs`.
- `e2e/fresh-app/templates/static/index.html` — the 20-line page with `<h1>` and a form (R2-00 task 3).
- `e2e/scripts/serve-dir.mjs` (task 3b), `e2e/scripts/set-branding.mjs`, `e2e/scripts/reset-branding.mjs` (task 4, **Decision:** under `scripts/` per harness §4).
- `e2e/scripts/lib/cli.mjs` (`spawnCli`, `CLI_ENTRY`), `lib/constants.mjs` (`PORTS.freshPreview = 4174`).
- `run-e2e.sh` — `--fresh` and `--whitelabel` flags; the fresh-app stacks run **serially** (shared port 4174); whitelabel phase wraps everything after branding-PUT in `try { … } finally { node e2e/scripts/reset-branding.mjs; node e2e/scripts/assert-branding-default.mjs; }`.
- `.github/workflows/e2e.yml` — job `fresh-app` matrix `stack: [vite, static, angular, next]` (**Decision:** `next` is a first-class matrix entry — the design table's omission is superseded) and job `whitelabel`, both `needs: e2e`, own reset+seed, `schedule` + `workflow_dispatch`; on `pull_request` (paths `cli/**`) the matrix is filtered to **`static` only** (5.1).

## Not covered here

- Scenarios R2-00 only *hosts* for other docs (each has its own spec file and doc): `init-yes-ci` → R1-02-tests (`R1-02-04`), `doctor-green-after-init` / `doctor-detects-tracked-credentials` → R1-04-tests (`R1-04-02/03`) — R2-00-01/02 step 4 exercises the green path incidentally, `origin-enforced-*` / `comment-burst-429` → R1-05-tests, `legacy-key-still-logs-in-after-upgrade` / `regenerated-key-old-one-rejected` → R1-06-tests, `quickstart-copies-prefilled-command` → dashboard repo (`DASH-R1-03-02`, SKIP here per harness §10).
- Unit-level: none new (R2-00 has "Unit: none"); the generator matrix itself is CI wiring, asserted by AC-6's green dispatch run, not a spec row.
- Manual: running one stack locally by hand — documented in `e2e/README.md`, not a scenario.

## Flake notes

- npm scaffolding nondeterminism → pinned **minor** generator versions + npm cache; exactly one whole-stack retry on the 300 s budget (harness §9).
- `vite preview` and `serve-dir.mjs` both use `--strictPort` 4174; started/killed by `run.mjs` `finally` so a failed stack never leaks the port into the whitelabel phase. **The stacks therefore run serially** — never two fresh-app stacks in parallel on one runner.
- Widget boot race: **register** `waitForResponse('**/capture-config')` before the action that triggers it — `fetchCaptureConfig()` runs inside `init()`, which `_boot` calls only when a token already exists (`element.ts:266-267`); in a fresh-login flow it fires from the post-login callback (`element.ts:673`), so a waiter registered afterwards hangs to timeout (`widget.spec.ts:61-62` is the correct pattern).
- The fresh apps inject the widget with `environment=local`, so comments land in env 1 — assert with the unfiltered API list or `environment=1`; in the widget, `fetchComments()` always filters by `environmentInt` (`element.ts:618`).
- Brand-window ordering: R2-00-06/08 contexts must be created **after** the branding PUT (`loadBranding()` runs once per boot, `element.ts:263`).
- R2-00-06/08 must run **inside** the R2-00-05 brand window and before `reset-branding.mjs`; ordering is `run.mjs --brand-check` (CLI) → widget specs → `finally` reset → R2-00-07 assert.
- `next`/`angular` scaffolds are nightly-only (runtime + npm variability); PR runs only execute them when `cli/**` changed.
