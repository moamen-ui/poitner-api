import { test } from 'node:test';
import * as assert from 'node:assert';
import { api, ApiError } from '../src/api.js';

test('api throws ApiError on failure', async () => {
  // stub test
  assert.ok(true);
});
